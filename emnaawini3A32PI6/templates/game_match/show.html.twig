{% extends 'base.html.twig' %}

{% block title %}GameMatch{% endblock %}

{% block body %}
    <h1>GameMatch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_match</th>
                <td>{{ game_match.idMatch }}</td>
            </tr>
            <tr>
                <th>Date_match</th>
                <td>{{ game_match.dateMatch ? game_match.dateMatch|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Score_equipe1</th>
                <td>{{ game_match.scoreEquipe1 }}</td>
            </tr>
            <tr>
                <th>Score_equipe2</th>
                <td>{{ game_match.scoreEquipe2 }}</td>
            </tr>
            <tr>
                <th>Statut_match</th>
                <td>{{ game_match.statutMatch }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_game_match_index') }}">back to list</a>

    <a href="{{ path('app_game_match_edit', {'id': game_match.id}) }}">edit</a>

    {{ include('game_match/_delete_form.html.twig') }}
{% endblock %}
