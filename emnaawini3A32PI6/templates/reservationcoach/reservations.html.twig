<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ 'Espace coach'|trans|default('Espace coach') }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="Fitness, Gym, HIVE, Training">
    <meta name="description" content="HIVE - Your ultimate fitness destination by BEEU">

    <!-- Favicon -->
    <link href="{{ asset('img/favicon.ico') }}" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="{{ asset('lib/flaticon/font/flaticon.css') }}" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="{{ asset('lib/owlcarousel/assets/owl.carousel.min.css') }}" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        :root {
            --bleu-vif: #00BFFF; /* Bright blue for highlights */
            --bleu: #1E90FF; /* Primary blue for buttons and accents */
            --bleu-fonce: #0A4D68; /* Dark blue for depth */
            --bleu-clair: #E6F0FA; /* Light blue matching background */
            --bleu-moyen: #4682B4; /* Mid-tone blue for balance */
            --gris: #2A2A2A; /* Dark gray for text */
            --jaune: #FFD700; /* Gold for standout elements */
            --shadow: 0 10px 30px rgba(30, 144, 255, 0.2); /* Subtle blue shadow */
            --shadow-dark: 0 8px 20px rgba(10, 77, 104, 0.3); /* Darker blue shadow */
        }
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2); /* Smooth gradient */
            color: var(--gris);
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }
        /* Header */
        .bg-dark {
            background: var(--bleu-fonce) !important;
            box-shadow: var(--shadow-dark);
            transition: background 0.3s ease;
        }
        .bg-secondary {
            background: var(--bleu-clair) !important;
            color: var(--bleu-fonce);
            border-bottom: 1px solid rgba(70, 130, 180, 0.2); /* Subtle mid-blue border */
        }
        .navbar-brand {
            display: flex;
            align-items: center;
            transition: transform 0.3s ease, filter 0.3s ease;
        }
        .navbar-brand:hover {
            transform: scale(1.05);
            filter: brightness(1.1);
        }
        .navbar-brand h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-weight: 700;
            font-size: 2.5rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 2px 2px 6px rgba(0, 191, 255, 0.4);
            margin: 0;
        }
        .navbar-brand img {
            max-height: 80px;
            margin-right: 15px;
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            transition: transform 0.3s ease;
        }
        .navbar-brand:hover img {
            transform: rotate(5deg);
        }
        .nav-link {
            color: var(--bleu-clair) !important;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            font-size: 1.1rem;
            padding: 10px 15px;
            transition: color 0.3s ease, background 0.3s ease;
        }
        .nav-link:hover, .nav-link.active {
            color: var(--bleu-vif) !important;
            background: rgba(0, 191, 255, 0.1); /* Subtle hover background */
            border-radius: 5px;
        }
        .dropdown-menu {
            background: var(--bleu-fonce);
            border: none;
            box-shadow: var(--shadow-dark);
        }
        .dropdown-item {
            color: var(--bleu-clair);
            font-weight: 400;
            transition: background 0.3s ease, color 0.3s ease;
        }
        .dropdown-item:hover {
            background: var(--bleu);
            color: #fff;
        }
        /* Nested Dropdown */
        .dropdown-menu .dropdown-menu {
            top: 0;
            left: 100%;
            margin-left: 0.1rem;
            background: var(--bleu-fonce);
            box-shadow: var(--shadow-dark);
        }
        .dropdown-menu .dropdown:hover > .dropdown-menu {
            display: block;
        }
        /* Buttons */
        .btn-primary {
            background: var(--bleu);
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-primary:hover {
            background: var(--bleu-vif);
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 191, 255, 0.4);
        }
        .btn-outline-secondary {
            border-color: var(--gris);
            color: var(--gris);
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 500;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-outline-secondary:hover {
            background: var(--gris);
            border-color: var(--gris);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(42, 42, 42, 0.4);
        }
        .btn-success {
            background: #28a745;
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-success:hover {
            background: #218838;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(40, 167, 69, 0.4);
        }
        .btn-outline-danger {
            border-color: #dc3545;
            color: #dc3545;
            padding: 12px 20px;
            font-family: 'Oswald', sans-serif;
            font-weight: 500;
            font-size: 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-outline-danger:hover {
            background: #dc3545;
            border-color: #dc3545;
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(220, 53, 69, 0.4);
        }
        .btn-light {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-light:hover {
            background: var(--bleu-moyen);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(70, 130, 180, 0.3);
        }
        /* Social Media Icons */
        .btn-square {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            width: 40px;
            height: 40px;
            line-height: 40px;
            border-radius: 50%;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-square:hover {
            background: var(--bleu-vif);
            color: #fff;
            transform: scale(1.1);
            box-shadow: 0 8px 15px rgba(0, 191, 255, 0.4);
        }
        /* Contact Info */
        .bg-secondary h6 {
            font-family: 'Poppins', sans-serif;
            font-weight: 400;
            font-size: 0.95rem;
            margin: 0;
        }
        .bg-secondary i {
            color: var(--bleu-vif);
            font-size: 1.2rem;
            margin-right: 8px;
            transition: transform 0.3s ease;
        }
        .bg-secondary div:hover i {
            transform: scale(1.2);
        }
        /* Carousel */
        .carousel-caption {
            background: rgba(10, 77, 104, 0.75); /* Slightly more opaque */
            padding: 25px 50px;
            border-radius: 12px;
            box-shadow: var(--shadow-dark);
            animation: fadeInUp 1s ease;
        }
        .carousel-caption h5 {
            color: var(--jaune);
            font-family: 'Oswald', sans-serif;
            font-size: 1.6rem;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 15px;
        }
        .carousel-caption h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-size: 3.8rem;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 3px 3px 8px rgba(0, 191, 255, 0.5);
            margin-bottom: 25px;
        }
        .carousel-control-prev, .carousel-control-next {
            width: 5%;
            opacity: 0.8;
            transition: opacity 0.3s ease;
        }
        .carousel-control-prev:hover, .carousel-control-next:hover {
            opacity: 1;
        }
        .carousel-control-prev-icon, .carousel-control-next-icon {
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            width: 40px;
            height: 40px;
        }
        /* Coach Reservations Specific Styles */
        .container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 1.5rem;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
        }
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: var(--shadow-dark);
        }
        .card-header {
            background: var(--bleu-fonce);
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1.25rem;
            letter-spacing: 0.5px;
            color: var(--bleu-clair);
        }
        .table {
            background: #fff;
            border-radius: 8px;
        }
        .table-dark {
            background: var(--bleu-fonce);
            border-bottom: 2px solid var(--bleu-moyen);
        }
        .table-hover tbody tr:hover {
            background: rgba(0, 191, 255, 0.1);
            transition: background-color 0.3s ease;
        }
        .sortable {
            cursor: pointer;
            user-select: none;
            transition: background-color 0.3s ease;
        }
        .sortable:hover {
            background-color: var(--bleu-moyen) !important;
        }
        .sortable.asc .sort-indicator::after { content: ' ↑'; }
        .sortable.desc .sort-indicator::after { content: ' ↓'; }
        .sortable .sort-indicator::after {
            content: ' ⇅';
            font-size: 0.9em;
            opacity: 0.7;
            color: var(--bleu-clair);
            transition: opacity 0.2s;
        }
        .sortable:hover .sort-indicator::after {
            opacity: 1;
        }
        .badge {
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            padding: 6px 12px;
            border-radius: 5px;
        }
        .alert {
            border-radius: 8px;
            box-shadow: var(--shadow);
        }
        .alert-success {
            background: #d4edda;
            color: #155724;
        }
        .alert-danger {
            background: #f8d7da;
            color: #721c24;
        }
        .alert-info {
            background: #d1ecf1;
            color: #0c5460;
        }
        .input-group-text {
            background: var(--bleu-clair);
            border-color: var(--bleu-moyen);
            color: var(--bleu-fonce);
        }
        .form-control {
            font-family: 'Poppins', sans-serif;
            border-color: var(--bleu-moyen);
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .form-control:focus {
            border-color: var(--bleu-vif);
            box-shadow: 0 0 0 0.25rem rgba(0, 191, 255, 0.25);
        }
        .spinner-border {
            border-color: var(--bleu-vif);
            border-right-color: transparent;
        }
        /* Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(60px); }
            to { opacity: 1; transform: translateY(0); }
        }
        /* Responsive Adjustments */
        @media (max-width: 991px) {
            .navbar-brand h1 {
                font-size: 2rem;
            }
            .navbar-brand img {
                max-height: 60px;
            }
            .nav-link {
                font-size: 1rem;
                padding: 8px 12px;
            }
            .carousel-caption h1 {
                font-size: 2.5rem;
            }
            .carousel-caption h5 {
                font-size: 1.2rem;
            }
            .btn-primary, .btn-success, .btn-outline-secondary, .btn-outline-danger, .btn-light {
                padding: 10px 20px;
                font-size: 0.9rem;
            }
            .dropdown-menu .dropdown-menu {
                left: 0;
                margin-left: 0;
            }
            .card-header {
                font-size: 1.1rem;
            }
            .table {
                font-size: 0.9rem;
            }
        }
        @media (max-width: 576px) {
            .carousel-caption {
                padding: 15px 25px;
            }
            .carousel-caption h1 {
                font-size: 1.8rem;
            }
            .carousel-caption h5 {
                font-size: 1rem;
            }
            .container {
                padding: 1rem;
            }
            .input-group {
                flex-direction: column;
            }
            .input-group-text, .form-control, .btn {
                width: 100%;
                margin-bottom: 0.5rem;
            }
            .btn-group .btn {
                font-size: 0.8rem;
                padding: 6px 12px;
            }
            .table-responsive {
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header Start -->
    <div class="container-fluid bg-dark px-0 fixed-top">
        <div class="row gx-0">
            <div class="col-lg-3 bg-dark d-none d-lg-block">
                <a href="{{ path('app_home1') }}" class="navbar-brand w-100 h-100 m-0 p-0 d-flex align-items-center justify-content-center">
                    <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                    <h1>HIVE</h1>
                </a>
            </div>
            <div class="col-lg-9">
                <div class="row gx-0 bg-secondary d-none d-lg-flex animate__animated animate__fadeInDown">
                    <div class="col-lg-7 px-5 text-start">
                        <div class="h-100 d-inline-flex align-items-center py-3 me-4">
                            <i class="fa fa-envelope"></i>
                            <h6>info@hivebybeeu.com</h6>
                        </div>
                        <div class="h-100 d-inline-flex align-items-center py-3">
                            <i class="fa fa-phone-alt"></i>
                            <h6>+21622820210</h6>
                        </div>
                    </div>
                    <div class="col-lg-5 px-5 text-end">
                        <div class="d-inline-flex align-items-center py-3">
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a class="btn btn-square rounded-circle animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-youtube"></i>
                            </a>
                        </div>
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg bg-dark navbar-dark p-3 p-lg-0 px-lg-5">
                    <a href="{{ path('app_home1') }}" class="navbar-brand d-block d-lg-none">
                        <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                        <h1>HIVE</h1>
                    </a>
                    <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                        <div class="navbar-nav mr-auto py-0">
                            <a href="{{ path('app_home1') }}" class="nav-item nav-link">Home</a>
                            <a href="{{ path('app_about1') }}" class="nav-item nav-link">About</a>
                            <a href="{{ path('app_class') }}" class="nav-item nav-link">Classes</a>
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle active" data-bs-toggle="dropdown">Reserver</a>
                                <div class="dropdown-menu rounded-0 m-0">
                                    <div class="dropdown">
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('client_services') }}" class="dropdown-item">Services Client</a>
                                            <a href="{{ path('app_client_reservation_index') }}" class="dropdown-item">Abonnement</a>
                                            <form method="post" action="{{ path('app_devenir_participant', {'user_id': 8}) }}" style="display: inline;">
                                                <input type="hidden" name="_token" value="{{ csrf_token('devenir_participant8') }}">
                                                <button type="submit" class="dropdown-item" style="border: none; background: none; padding: 0; cursor: pointer;">Participation</button>
                                            </form>
                                        </div>
                                    </div>
                                    <div class="dropdown">
                                        <a href="#" class="dropdown-item dropdown-toggle" data-bs-toggle="dropdown">Coach</a>
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('app_coach_reservations') }}" class="dropdown-item">Espace coach</a>
                                            <a href="{{ path('app_planning_index',{'coachId': 2}) }}" class="dropdown-item">Planning</a>
                                            <a href="{{ path('app_cours_index', {'user_id': 2}) }}" class="dropdown-item">Cours</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
    <!-- Header End -->

    <!-- Carousel Start -->
    <div class="container-fluid p-0 mb-5">
        <div id="header-carousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="w-100" src="{{ asset('img/carousel-1.jpg') }}" alt="Build Your Strength" style="object-fit: cover; height: 100vh; filter: brightness(0.8) contrast(1.1);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="animate__animated animate__fadeIn">Best Fitness Experience</h5>
                            <h1 class="mb-md-4 animate__animated animate__zoomIn">Forge Your Body with HIVE</h1>
                            <a href="#" class="btn btn-primary py-md-3 px-md-5 me-3 animate__animated animate__bounceIn">Join Now</a>
                            <a href="{{ path('app_contact1') }}" class="btn btn-light py-md-3 px-md-5 animate__animated animate__bounceIn">Contact Us</a>
                        </div>
                    </div>
                </div>
                <div class="carousel-item">
                    <img class="w-100" src="{{ asset('img/carousel-2.jpg') }}" alt="Grow Stronger" style="object-fit: cover; height: 100vh; filter: brightness(0.8) contrast(1.1);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="animate__animated animate__fadeIn">Unleash Your Potential</h5>
                            <h1 class="mb-md-4 animate__animated animate__zoomIn">Strength Begins at HIVE</h1>
                            <a href="#" class="btn btn-primary py-md-3 px-md-5 me-3 animate__animated animate__bounceIn">Join Now</a>
                            <a href="{{ path('app_contact1') }}" class="btn btn-light py-md-3 px-md-5 animate__animated animate__bounceIn">Contact Us</a>
                        </div>
                    </div>
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#header-carousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#header-carousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>
    <!-- Carousel End -->

    <!-- Coach Reservations Section -->
    <div class="container my-5">
        {# Header Section #}
        <div class="d-flex justify-content-between align-items-center mb-4 animate__animated animate__fadeIn">
            <h1 class="text-warning fw-bold display-5 d-flex align-items-center gap-3">
                <i class="fas fa-running fa-beat"></i> {{ 'Espace coach'|trans|default('Espace coach') }}
            </h1>
            <span class="badge bg-info text-white px-3 py-2 rounded-pill">
                <i class="fas fa-clock me-1"></i> {{ "now"|date("d/m/Y H:i") }}
            </span>
        </div>

        {# Search Form #}
        <form method="get" action="{{ path('app_coach_reservations') }}" class="mb-4" id="searchForm">
            <div class="input-group shadow-sm rounded-3 animate__animated animate__fadeInUp">
                <span class="input-group-text bg-light border-0"><i class="fas fa-search"></i></span>
                <input type="text" 
                       name="search" 
                       value="{{ search|default('') }}" 
                       class="form-control border-0 py-2" 
                       placeholder="{{ 'Rechercher par nom d\'abonnement'|trans|default('Rechercher par nom d\'abonnement') }}" 
                       aria-label="{{ 'Rechercher des abonnements'|trans|default('Rechercher des abonnements') }}" 
                       id="searchInput">
                <button type="submit" class="btn btn-primary px-4" aria-label="{{ 'Lancer la recherche'|trans|default('Lancer la recherche') }}">
                    <i class="fas fa-search me-1"></i> {{ 'Rechercher'|trans|default('Rechercher') }}
                </button>
                <button type="button" 
                        class="btn btn-outline-danger px-3" 
                        onclick="document.getElementById('searchInput').value=''; this.form.submit();"
                        aria-label="{{ 'Effacer la recherche'|trans|default('Effacer la recherche') }}">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        </form>

        {# Action Buttons #}
        <div class="d-flex flex-wrap gap-3 mb-5 animate__animated animate__fadeInUp animate__delay-1s">
            <a href="{{ path('app_coach_reservations_refresh') }}" 
               class="btn btn-outline-secondary shadow-sm px-4 py-2" 
               id="refreshBtn">
                <i class="fas fa-sync-alt me-1"></i> {{ 'Actualiser'|trans|default('Actualiser') }}
            </a>
            <a href="{{ path('app_coach_reservations_export') }}" 
               class="btn btn-success shadow-sm px-4 py-2" 
               title="{{ 'Télécharger les données au format Excel'|trans|default('Télécharger les données au format Excel') }}">
                <i class="fas fa-file-excel me-1"></i> {{ 'Exporter en Excel'|trans|default('Exporter en Excel') }}
            </a>
        </div>

        {# Flash Messages #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show shadow-sm mb-4 animate__animated animate__fadeIn" role="alert">
                    <i class="fas fa-{{ label == 'success' ? 'check-circle' : label == 'danger' ? 'exclamation-circle' : 'info-circle' }} me-2"></i>
                    {{ message|trans }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="{{ 'Fermer'|trans|default('Fermer') }}"></button>
                </div>
            {% endfor %}
        {% endfor %}

        {# Reservations Table #}
        <div class="card shadow-lg rounded-3 overflow-hidden animate__animated animate__fadeInUp animate__delay-2s">
            <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center py-3">
                <h5 class="mb-0 fw-semibold">{{ 'Liste des Réservations'|trans|default('Liste des Réservations') }}</h5>
                <span class="badge bg-secondary rounded-pill px-3 py-1">{{ reservations|length }} {{ 'abonnements'|trans|default('abonnements') }}</span>
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle mb-0">
                        <thead class="table-dark sticky-top">
                            <tr>
                                <th scope="col" class="sortable py-3" data-sort="subscriptionName">
                                    {{ 'Nom de l\'Abonnement'|trans|default('Nom de l\'Abonnement') }}
                                    <span class="sort-indicator ms-2"></span>
                                </th>
                                <th scope="col" class="text-center sortable py-3" data-sort="clientCount">
                                    {{ 'Nombre de Clients'|trans|default('Nombre de Clients') }}
                                    <span class="sort-indicator ms-2"></span>
                                </th>
                            </tr>
                        </thead>
                        <tbody id="reservationTableBody">
                            {% if reservations is not empty %}
                                {% for reservation in reservations %}
                                    <tr class="animate__animated animate__fadeIn animate__delay-3s">
                                        <td class="py-3">{{ reservation.subscriptionName }}</td>
                                        <td class="text-center fw-medium py-3">{{ reservation.clientCount }}</td>
                                    </tr>
                                {% endfor %}
                            {% else %}
                                <tr>
                                    <td colspan="2" class="text-muted text-center py-5">
                                        <i class="fas fa-emoji-frown me-2 fs-3"></i> {{ 'Aucune réservation trouvée'|trans|default('Aucune réservation trouvée') }}.
                                    </td>
                                </tr>
                            {% endif %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        {# Loading Spinner #}
        <div id="loading" class="text-center mt-4 d-none">
            <div class="spinner-border text-primary" role="status" style="width: 3rem; height: 3rem;">
                <span class="visually-hidden">{{ 'Chargement...'|trans|default('Chargement...') }}</span>
            </div>
            <p class="text-muted mt-2 fs-5">{{ 'Chargement des données...'|trans|default('Chargement des données...') }}</p>
        </div>
    </div>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('lib/easing/easing.min.js') }}"></script>
    <script src="{{ asset('lib/waypoints/waypoints.min.js') }}"></script>
    <script src="{{ asset('lib/counterup/counterup.min.js') }}"></script>
    <script src="{{ asset('lib/owlcarousel/owl.carousel.min.js') }}"></script>

    <!-- Template Javascript -->
    <script src="{{ asset('js/main.js') }}"></script>

    <!-- Custom JavaScript -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Smooth scroll for nav links
            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', function(e) {
                    if (this.getAttribute('href').startsWith('#')) {
                        e.preventDefault();
                        const target = document.querySelector(this.getAttribute('href'));
                        target.scrollIntoView({ behavior: 'smooth' });
                    }
                });
            });

            // Enable Bootstrap tooltips
            $('[data-bs-toggle="tooltip"]').tooltip();

            // Show loading spinner during actions
            const showLoading = () => document.getElementById('loading').classList.remove('d-none');
            const hideLoading = () => document.getElementById('loading').classList.add('d-none');

            // Form submission and refresh button
            document.getElementById('searchForm').addEventListener('submit', showLoading);
            document.getElementById('refreshBtn').addEventListener('click', showLoading);

            // Client-side sorting
            const tableBody = document.getElementById('reservationTableBody');
            const sortables = document.querySelectorAll('.sortable');
            
            sortables.forEach(th => {
                th.addEventListener('click', () => {
                    const sortKey = th.dataset.sort;
                    const rows = Array.from(tableBody.querySelectorAll('tr'));
                    const isAscending = th.classList.toggle('asc');

                    rows.sort((a, b) => {
                        const aValue = sortKey === 'clientCount' 
                            ? parseInt(a.cells[1].textContent) 
                            : a.cells[0].textContent.toLowerCase();
                        const bValue = sortKey === 'clientCount' 
                            ? parseInt(b.cells[1].textContent) 
                            : b.cells[0].textContent.toLowerCase();
                        
                        return isAscending 
                            ? (aValue > bValue ? 1 : -1) 
                            : (aValue < bValue ? 1 : -1);
                    });

                    rows.forEach(row => tableBody.appendChild(row));
                    sortables.forEach(s => s !== th && s.classList.remove('asc', 'desc'));
                    th.classList.add(isAscending ? 'asc' : 'desc');
                });
            });
        });
    </script>
</body>
</html>