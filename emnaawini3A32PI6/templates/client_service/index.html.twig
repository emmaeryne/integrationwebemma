<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ 'Espace client'|trans|default('Services pour clients') }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="Fitness, Gym, HIVE, Training">
    <meta name="description" content="HIVE - Your ultimate fitness destination by BEEU">

    <!-- Favicon -->
    <link href="{{ asset('img/favicon.ico') }}" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="{{ asset('lib/flaticon/font/flaticon.css') }}" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="{{ asset('lib/owlcarousel/assets/owl.carousel.min.css') }}" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        :root {
            --bleu-vif: #00BFFF;
            --bleu: #1E90FF;
            --bleu-fonce: #0A4D68;
            --bleu-clair: #E6F0FA;
            --bleu-moyen: #4682B4;
            --gris: #2A2A2A;
            --jaune: #FFD700;
            --shadow: 0 10px 30px rgba(30, 144, 255, 0.2);
            --shadow-dark: 0 8px 20px rgba(10, 77, 104, 0.3);
        }
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
            color: var(--gris);
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }
        /* Header */
        .bg-dark {
            background: var(--bleu-fonce) !important;
            box-shadow: var(--shadow-dark);
            transition: background 0.3s ease;
        }
        .bg-secondary {
            background: var(--bleu-clair) !important;
            color: var(--bleu-fonce);
            border-bottom: 1px solid rgba(70, 130, 180, 0.2);
        }
        .navbar-brand {
            display: flex;
            align-items: center;
            transition: transform 0.3s ease, filter 0.3s ease;
        }
        .navbar-brand:hover {
            transform: scale(1.05);
            filter: brightness(1.1);
        }
        .navbar-brand h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-weight: 700;
            font-size: 2.5rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 2px 2px 6px rgba(0, 191, 255, 0.4);
            margin: 0;
        }
        .navbar-brand img {
            max-height: 80px;
            margin-right: 15px;
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            transition: transform 0.3s ease;
        }
        .navbar-brand:hover img {
            transform: rotate(5deg);
        }
        .nav-link {
            color: var(--bleu-clair) !important;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            font-size: 1.1rem;
            padding: 10px 15px;
            transition: color 0.3s ease, background 0.3s ease;
        }
        .nav-link:hover, .nav-link.active {
            color: var(--bleu-vif) !important;
            background: rgba(0, 191, 255, 0.1);
            border-radius: 5px;
        }
        .dropdown-menu {
            background: var(--bleu-fonce);
            border: none;
            box-shadow: var(--shadow-dark);
        }
        .dropdown-item {
            color: var(--bleu-clair);
            font-weight: 400;
            transition: background 0.3s ease, color 0.3s ease;
        }
        .dropdown-item:hover {
            background: var(--bleu);
            color: #fff;
        }
        .dropdown-menu .dropdown-menu {
            top: 0;
            left: 100%;
            margin-left: 0.1rem;
            background: var(--bleu-fonce);
            box-shadow: var(--shadow-dark);
        }
        .dropdown-menu .dropdown:hover > .dropdown-menu {
            display: block;
        }
        .btn-primary {
            background: var(--bleu);
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-primary:hover {
            background: var(--bleu-vif);
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 191, 255, 0.4);
        }
        .btn-outline-primary {
            border-color: var(--bleu);
            color: var(--bleu);
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-outline-primary:hover {
            background: var(--bleu-vif);
            border-color: var(--bleu-vif);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 191, 255, 0.4);
        }
        .btn-light {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-light:hover {
            background: var(--bleu-moyen);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(70, 130, 180, 0.3);
        }
        .btn-square {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            width: 40px;
            height: 40px;
            line-height: 40px;
            border-radius: 50%;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-square:hover {
            background: var(--bleu-vif);
            color: #fff;
            transform: scale(1.1);
            box-shadow: 0 8px 15px rgba(0, 191, 255, 0.4);
        }
        .bg-secondary h6 {
            font-family: 'Poppins', sans-serif;
            font-weight: 400;
            font-size: 0.95rem;
            margin: 0;
        }
        .bg-secondary i {
            color: var(--bleu-vif);
            font-size: 1.2rem;
            margin-right: 8px;
            transition: transform 0.3s ease;
        }
        .bg-secondary div:hover i {
            transform: scale(1.2);
        }
        .carousel-caption {
            background: rgba(10, 77, 104, 0.75);
            padding: 25px 50px;
            border-radius: 12px;
            box-shadow: var(--shadow-dark);
            animation: fadeInUp 1s ease;
        }
        .carousel-caption h5 {
            color: var(--jaune);
            font-family: 'Oswald', sans-serif;
            font-size: 1.6rem;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 15px;
        }
        .carousel-caption h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-size: 3.8rem;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 3px 3px 8px rgba(0, 191, 255, 0.5);
            margin-bottom: 25px;
        }
        .carousel-control-prev, .carousel-control-next {
            width: 5%;
            opacity: 0.8;
            transition: opacity 0.3s ease;
        }
        .carousel-control-prev:hover, .carousel-control-next:hover {
            opacity: 1;
        }
        .carousel-control-prev-icon, .carousel-control-next-icon {
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            width: 40px;
            height: 40px;
        }
        .container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 1.5rem;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }
        .transition-all {
            transition: all 0.3s ease;
        }
        .hover-scale:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        .weather-card {
            background: linear-gradient(145deg, #ffffff, #f0f0f0);
            position: relative;
            overflow: hidden;
            border: 2px solid var(--bleu);
        }
        .weather-result {
            background: rgba(255, 255, 255, 0.9);
            transition: background 0.5s ease;
            border-radius: 1rem;
        }
        .weather-icon {
            color: var(--bleu);
        }
        .weather-icon-img {
            width: 60px;
            height: 60px;
        }
        .weather-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0.1;
            z-index: 0;
            transition: opacity 0.5s ease;
        }
        .weather-card.clear::before {
            background: url('https://www.transparenttextures.com/patterns/clear-sky.png');
            opacity: 0.3;
        }
        .weather-card.clouds::before {
            background: url('https://www.transparenttextures.com/patterns/cloudy.png');
            opacity: 0.3;
        }
        .weather-card.rain::before {
            background: url('https://www.transparenttextures.com/patterns/rain.png');
            opacity: 0.3;
        }
        .card-body {
            position: relative;
            z-index: 1;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 1rem;
        }
        .form-select, .btn {
            transition: all 0.3s ease;
        }
        .form-select:focus, .btn:focus {
            box-shadow: 0 0 0 0.25rem rgba(30, 144, 255, 0.25);
        }
        .badge {
            padding: 0.5rem 1rem;
            font-size: 0.9rem;
            font-family: 'Poppins', sans-serif;
        }
        .alert {
            border-radius: 8px;
            box-shadow: var(--shadow);
        }
        .alert-success {
            background: #d4edda;
            color: #155724;
        }
        .alert-danger {
            background: #f8d7da;
            color: #721c24;
        }
        .form-label {
            font-family: 'Poppins', sans-serif;
            color: var(--gris);
        }
        .form-range::-webkit-slider-thumb {
            background: var(--bleu-vif);
            box-shadow: 0 0 8px rgba(0, 191, 255, 0.4);
        }
        .form-range::-moz-range-thumb {
            background: var(--bleu-vif);
            box-shadow: 0 0 8px rgba(0, 191, 255, 0.4);
        }
        /* Modal Styles */
        .modal-content {
            border-radius: 1rem;
            box-shadow: var(--shadow-dark);
        }
        .modal-header {
            background: var(--bleu);
            color: #fff;
            border-top-left-radius: 1rem;
            border-top-right-radius: 1rem;
            border-bottom: 4px solid var(--bleu-vif);
        }
        .modal-title {
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
        }
        .modal-body {
            background: var(--bleu-clair);
            padding: 2rem;
        }
        .modal-footer {
            border-top: none;
            justify-content: space-between;
        }
        .detail-item {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }
        .label {
            font-weight: 600;
            color: #2c3e50;
            min-width: 150px;
        }
        .value {
            color: #222;
            flex: 1;
        }
        .status-badge {
            padding: 5px 10px;
            border-radius: 12px;
            font-size: 0.9em;
        }
        .status-active {
            background: #d4edda;
            color: #155724;
        }
        .status-inactive {
            background: #f8d7da;
            color: #721c24;
        }
        /* Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(60px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @media (max-width: 991px) {
            .navbar-brand h1 {
                font-size: 2rem;
            }
            .navbar-brand img {
                max-height: 60px;
            }
            .nav-link {
                font-size: 1rem;
                padding: 8px 12px;
            }
            .carousel-caption h1 {
                font-size: 2.5rem;
            }
            .carousel-caption h5 {
                font-size: 1.2rem;
            }
            .btn-primary, .btn-light, .btn-outline-primary {
                padding: 10px 20px;
                font-size: 0.9rem;
            }
            .dropdown-menu .dropdown-menu {
                left: 0;
                margin-left: 0;
            }
        }
        @media (max-width: 576px) {
            .carousel-caption {
                padding: 15px 25px;
            }
            .carousel-caption h1 {
                font-size: 1.8rem;
            }
            .carousel-caption h5 {
                font-size: 1rem;
            }
            .container {
                padding: 1rem;
            }
            .card-body {
                padding: 1.5rem;
            }
            .form-select-lg {
                font-size: 0.9rem;
            }
            .btn-lg {
                font-size: 0.9rem;
                padding: 8px 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Header Start -->
    <div class="container-fluid bg-dark px-0 fixed-top">
        <div class="row gx-0">
            <div class="col-lg-3 bg-dark d-none d-lg-block">
                <a href="{{ path('app_home1') }}" class="navbar-brand w-100 h-100 m-0 p-0 d-flex align-items-center justify-content-center">
                    <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                    <h1>HIVE</h1>
                </a>
            </div>
            <div class="col-lg-9">
                <div class="row gx-0 bg-secondary d-none d-lg-flex animate__animated animate__fadeInDown">
                    <div class="col-lg-7 px-5 text-start">
                        <div class="h-100 d-inline-flex align-items-center py-3 me-4">
                            <i class="fa fa-envelope"></i>
                            <h6>info@hivebybeeu.com</h6>
                        </div>
                        <div class="h-100 d-inline-flex align-items-center py-3">
                            <i class="fa fa-phone-alt"></i>
                            <h6>+21622820210</h6>
                        </div>
                    </div>
                    <div class="col-lg-5 px-5 text-end">
                        <div class="d-inline-flex align-items-center py-3">
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a class="btn btn-square rounded-circle animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-youtube"></i>
                            </a>
                        </div>
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg bg-dark navbar-dark p-3 p-lg-0 px-lg-5">
                    <a href="{{ path('app_home1') }}" class="navbar-brand d-block d-lg-none">
                        <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                        <h1>HIVE</h1>
                    </a>
                    <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                        <div class="navbar-nav mr-auto py-0">
                            <a href="{{ path('app_home1') }}" class="nav-item nav-link">Home</a>
                            <a href="{{ path('app_about1') }}" class="nav-item nav-link">About</a>
                            <a href="{{ path('app_class') }}" class="nav-item nav-link">Classes</a>
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle active" data-bs-toggle="dropdown">Reserver</a>
                                <div class="dropdown-menu rounded-0 m-0">
                                    <div class="dropdown">
                                        <a href="#" class="dropdown-item dropdown-toggle" data-bs-toggle="dropdown">Client</a>
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('client_services') }}" class="dropdown-item active">Services Client</a>
                                            <a href="{{ path('app_client_reservation_index') }}" class="dropdown-item">Abonnement</a>
                                            <form method="post" action="{{ path('app_devenir_participant', {'user_id': app.user ? app.user.id : 8}) }}" style="display: inline;">
                                                <input type="hidden" name="_token" value="{{ csrf_token('devenir_participant' ~ (app.user ? app.user.id : 8)) }}">
                                                <button type="submit" class="dropdown-item" style="border: none; background: none; padding: 0; cursor: pointer;">Participation</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <a href="{{ path('app_contact1') }}" class="nav-item nav-link">Contact</a>
                        </div>
                        <a href="#" class="btn btn-primary py-md-3 px-md-5 d-none d-lg-block">Join Us</a>
                    </div>
                </nav>
            </div>
        </div>
    </div>
    <!-- Header End -->

    <!-- Carousel Start -->
    <div class="container-fluid p-0 mb-5">
        <div id="header-carousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="w-100" src="{{ asset('img/carousel-1.jpg') }}" alt="Build Your Strength" style="object-fit: cover; height: 100vh; filter: brightness(0.8) contrast(1.1);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="animate__animated animate__fadeIn">Best Fitness Experience</h5>
                            <h1 class="mb-md-4 animate__animated animate__zoomIn">Forge Your Body with HIVE</h1>
                            <a href="#" class="btn btn-primary py-md-3 px-md-5 me-3 animate__animated animate__bounceIn">Join Now</a>
                            <a href="{{ path('app_contact1') }}" class="btn btn-light py-md-3 px-md-5 animate__animated animate__bounceIn">Contact Us</a>
                        </div>
                    </div>
                </div>
                <div class="carousel-item">
                    <img class="w-100" src="{{ asset('img/carousel-2.jpg') }}" alt="Grow Stronger" style="object-fit: cover; height: 100vh; filter: brightness(0.8) contrast(1.1);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="animate__animated animate__fadeIn">Unleash Your Potential</h5>
                            <h1 class="mb-md-4 animate__animated animate__zoomIn">Strength Begins at HIVE</h1>
                            <a href="#" class="btn btn-primary py-md-3 px-md-5 me-3 animate__animated animate__bounceIn">Join Now</a>
                            <a href="{{ path('app_contact1') }}" class="btn btn-light py-md-3 px-md-5 animate__animated animate__bounceIn">Contact Us</a>
                        </div>
                    </div>
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#header-carousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#header-carousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>
    <!-- Carousel End -->

    <!-- Client Services Section -->
    <div class="container mt-5">
        <h1 class="mb-5 text-warning display-5 fw-bold d-flex align-items-center gap-3 animate__animated animate__fadeIn">
            <i class="fas fa-dumbbell fa-beat"></i>
            {{ 'Espace client'|trans|default('Services pour clients') }}
        </h1>

        {# Flash Messages #}
        {% for type, messages in app.flashes %}
            <div class="alert alert-{{ type }} fade show d-flex align-items-center shadow-lg mb-5 rounded-3 animate__animated animate__fadeIn" role="alert">
                <i class="fas fa-{{ type == 'success' ? 'check-circle' : 'exclamation-circle' }} me-3 text-{{ type }} fs-4 animate__animated animate__pulse"></i>
                <span class="fs-5">{{ messages|join(' ') }}</span>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}

        {# Service Selection and Rating Form #}
        <div class="card shadow-lg mb-5 border-0 rounded-4 animate__animated animate__fadeInUp">
            <div class="card-body p-5">
                <form method="post" action="{{ path('client_services') }}" class="row g-4">
                    <div class="col-md-6">
                        <label for="service_id" class="form-label fw-bold text-muted fs-5 d-flex align-items-center gap-2">
                            <i class="fas fa-concierge-bell text-primary fa-beat-fade"></i>
                            {{ 'Choisi un service'|trans|default('Sélectionner un service') }}
                        </label>
                        <div class="input-group">
                            <select name="service_id" id="service_id" class="form-select form-select-lg rounded-3 shadow-sm transition-all">
                                <option value="">{{ 'services'|trans|default('Choisir un service') }}</option>
                                {% for service in services %}
                                    <option value="{{ service.id }}"
                                            data-service='{{ {
                                                "id": service.id,
                                                "nom": service.nom|default('N/A'),
                                                "salleStatut": service.salle|default('N/A'),
                                                "description": service.description|default('Aucune description disponible')
                                            }|json_encode|e('html_attr') }}'
                                            {{ selectedService and selectedService.id == service.id ? 'selected' : '' }}>
                                        {{ service.nom }}
                                    </option>
                                {% endfor %}
                            </select>
                            <button type="button" class="btn btn-outline-primary rounded-3 ms-2" id="show-room-details" disabled>
                                <i class="bi bi-info-circle me-2"></i>{{ 'Détails de la salle'|trans|default('Détails de la salle') }}
                            </button>
                        </div>
                    </div>

                    <div class="col-md-6">
                        <label for="note" class="form-label fw-bold text-muted fs-5 d-flex align-items-center gap-2">
                            <i class="fas fa-star text-warning fa-spin"></i>
                            {{ 'attribuer note'|trans|default('Note (0-5)') }}
                        </label>
                        <div class="d-flex align-items-center gap-3">
                            <input type="range" name="note" id="note" min="0" max="5" step="0.1" value="0" class="form-range" oninput="this.nextElementSibling.value = this.value">
                            <output class="badge bg-warning text-dark px-3 py-2 rounded-pill animate__animated animate__bounce">0</output>
                        </div>
                    </div>

                    <div class="col-12 d-flex gap-3">
                        <button type="submit" class="btn btn-primary btn-lg shadow-lg rounded-pill px-5 py-2 fs-5 d-flex align-items-center gap-2 transition-all hover-scale">
                            <i class="fas fa-save fa-bounce"></i>
                            {{ 'reserve'|trans|default('Réserver') }}
                        </button>
                    </div>
                </form>

                {# Selected Service Details #}
                {% if selectedService %}
                    <div class="mt-4 p-4 bg-light rounded-3 shadow-sm animate__animated animate__fadeIn">
                        <h5 class="text-muted fs-4 d-flex align-items-center gap-2">
                            <i class="fas fa-info-circle text-primary fa-beat"></i>
                            {{ selectedService.nom }}
                        </h5>
                        <div class="row g-3">
                            <div class="col-md-6">
                                <p class="mb-2"><i class="fas fa-euro-sign text-success me-2 fa-spin"></i>{{ 'Prix'|trans|default('Prix') }} : <span class="badge bg-success text-white">{{ selectedService.prix ? selectedService.prix|number_format(2, ',', ' ') : 'N/A' }} €</span></p>
                                <p class="mb-2"><i class="fas fa-signal text-info me-2 fa-beat-fade"></i>{{ 'Niveau'|trans|default('Niveau') }} : <span class="badge bg-info text-white">{{ selectedService.niveauDifficulte|default('N/A') }}</span></p>
                                <p class="mb-2"><i class="fas fa-clock text-warning me-2 fa-spin"></i>{{ 'Durée'|trans|default('Durée') }} : <span class="badge bg-warning text-dark">{{ selectedService.dureeMinutes|default('N/A') }} {{ 'minutes'|trans|default('minutes') }}</span></p>
                            </div>
                            {% if gifUrl %}
                                <div class="col-md-6 text-center">
                                    <img src="{{ gifUrl }}" alt="GIF du service" class="img-fluid rounded-3 shadow-lg animate__animated animate__zoomIn" style="max-height: 200px;">
                                </div>
                            {% endif %}
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>

        {# Room Details Modal #}
        <div class="modal fade" id="roomDetailsModal" tabindex="-1" aria-labelledby="roomDetailsModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-lg modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="roomDetailsModalLabel">
                            <i class="bi bi-info-circle me-2"></i>{{ 'Détails de la salle'|trans|default('Détails de la salle') }}
                        </h5>
                        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <div class="row">
                            <div class="col-12">
                                <div class="detail-item">
                                    <span class="label">{{ 'Statut de la salle'|trans|default('Statut de la salle') }} :</span>
                                    <span class="value">
                                        <span class="status-badge" id="modal-salle-statut">N/A</span>
                                    </span>
                                </div>
                                <div class="detail-item">
                                    <span class="label">{{ 'Description'|trans|default('Description') }} :</span>
                                    <span class="value" id="modal-description">{{ 'Aucune description disponible'|trans|default('Aucune description disponible') }}</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-primary" data-bs-dismiss="modal" style="transition: all 0.3s ease;" onmouseover="this.style.transform='translateY(-2px)';" onmouseout="this.style.transform='translateY(0)';">
                            <i class="bi bi-x-circle me-2"></i>{{ 'Fermer'|trans|default('Fermer') }}
                        </button>
                    </div>
                </div>
            </div>
        </div>

        {# Weather Section #}
        <div class="card shadow-lg mb-5 border-0 rounded-4 animate__animated animate__fadeInUp weather-card d-none" id="weather-card">
            <div class="card-body p-5">
                <h5 class="text-muted mb-4 fs-4 d-flex align-items-center gap-2">
                    <i class="fas fa-cloud-sun text-primary fa-beat"></i>
                    {{ 'Méteo'|trans|default('Météo') }}
                </h5>
                <button id="get-weather-btn" class="btn btn-outline-primary btn-lg rounded-pill px-4 py-2 fs-5 shadow-lg transition-all hover-scale">
                    <i class="fas fa-cloud-download-alt me-2 fa-bounce"></i>
                    {{ 'voir méteo'|trans|default('Obtenir la météo') }}
                </button>
                <div id="weather-result" class="mt-4 p-4 rounded-3 shadow-sm weather-result">
                    {% if weatherData %}
                        <div class="d-flex align-items-center gap-3 animate__animated animate__fadeIn">
                            <i class="weather-icon fas fa-{{ weatherData.weather[0].main|lower == 'clear' ? 'sun' : (weatherData.weather[0].main|lower == 'clouds' ? 'cloud' : (weatherData.weather[0].main|lower == 'rain' ? 'cloud-rain' : 'cloud-sun')) }} fa-3x text-primary animate__animated animate__rotateIn"></i>
                            <div class="flex-grow-1">
                                <p class="mb-1 fs-5"><i class="fas fa-thermometer-half text-danger me-2 fa-beat"></i>{{ 'Température'|trans|default('Température') }} : <span class="badge bg-danger text-white">{{ weatherData.main.temp }}°C</span></p>
                                <p class="mb-0 fs-5"><i class="fas fa-cloud text-primary me-2 fa-beat-fade"></i>{{ 'Conditions'|trans|default('Conditions') }} : <span class="badge bg-primary text-white">{{ weatherData.weather[0].description|capitalize }}</span></p>
                            </div>
                            <img src="http://openweathermap.org/img/w/{{ weatherData.weather[0].icon }}.png" alt="Icône météo" class="weather-icon-img animate__animated animate__zoomIn">
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('lib/easing/easing.min.js') }}"></script>
    <script src="{{ asset('lib/waypoints/waypoints.min.js') }}"></script>
    <script src="{{ asset('lib/counterup/counterup.min.js') }}"></script>
    <script src="{{ asset('lib/owlcarousel/owl.carousel.min.js') }}"></script>

    <!-- Template Javascript -->
    <script src="{{ asset('js/main.js') }}"></script>

    <!-- Custom JavaScript -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Smooth scroll for nav links
            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', function(e) {
                    if (this.getAttribute('href').startsWith('#')) {
                        e.preventDefault();
                        const target = document.querySelector(this.getAttribute('href'));
                        target.scrollIntoView({ behavior: 'smooth' });
                    }
                });
            });

            // Enable Bootstrap tooltips
            $('[data-bs-toggle="tooltip"]').tooltip();

            // Weather AJAX
            const weatherBtn = document.getElementById('get-weather-btn');
            const weatherResult = document.getElementById('weather-result');
            const weatherCard = document.getElementById('weather-card');

            weatherBtn.addEventListener('click', () => {
                weatherResult.innerHTML = '<p class="text-muted animate__animated animate__fadeIn">{{ "Chargement..."|trans|default("Chargement...") }}</p>';
                fetch('{{ path('client_services_weather') }}')
                    .then(response => response.json())
                    .then(data => {
                        if (data.error) {
                            weatherResult.innerHTML = `<p class="text-danger animate__animated animate__fadeIn">${data.error}</p>`;
                        } else {
                            const condition = data.conditions.toLowerCase();
                            weatherCard.classList.remove('clear', 'clouds', 'rain');
                            if (condition.includes('clear')) {
                                weatherCard.classList.add('clear');
                            } else if (condition.includes('cloud')) {
                                weatherCard.classList.add('clouds');
                            } else if (condition.includes('rain')) {
                                weatherCard.classList.add('rain');
                            }

                            weatherResult.innerHTML = `
                                <div class="d-flex align-items-center gap-3 animate__animated animate__fadeIn">
                                    <i class="weather-icon fas fa-${
                                        condition.includes('clear') ? 'sun' :
                                        condition.includes('cloud') ? 'cloud' :
                                        condition.includes('rain') ? 'cloud-rain' : 'cloud-sun'
                                    } fa-3x text-primary animate__animated animate__rotateIn"></i>
                                    <div class="flex-grow-1">
                                        <p class="mb-1 fs-5"><i class="fas fa-thermometer-half text-danger me-2 fa-beat"></i>{{ 'Température'|trans|default('Température') }} : <span class="badge bg-danger text-white">${data.temperature}°C</span></p>
                                        <p class="mb-0 fs-5"><i class="fas fa-cloud text-primary me-2 fa-beat-fade"></i>{{ 'Conditions'|trans|default('Conditions') }} : <span class="badge bg-primary text-white">${data.conditions.charAt(0).toUpperCase() + data.conditions.slice(1)}</span></p>
                                    </div>
                                    <img src="http://openweathermap.org/img/w/${data.icon}.png" alt="Icône météo" class="weather-icon-img animate__animated animate__zoomIn">
                                </div>
                            `;
                        }
                    })
                    .catch(error => {
                        weatherResult.innerHTML = '<p class="text-danger animate__animated animate__fadeIn">{{ "Erreur lors de la récupération de la météo."|trans|default("Erreur lors de la récupération de la météo.") }}</p>';
                        console.error('Erreur:', error);
                    });
            });

            // Gestion du bouton "Détails de la salle" et affichage/masquage de la section météo
            const serviceSelect = document.getElementById('service_id');
            const showRoomDetailsBtn = document.getElementById('show-room-details');
            const modalElement = document.getElementById('roomDetailsModal');
            let modal;

            try {
                modal = new bootstrap.Modal(modalElement);
            } catch (error) {
                console.error('Erreur lors de l\'initialisation de la modale:', error);
                return;
            }

            serviceSelect.addEventListener('change', () => {
                const selectedOption = serviceSelect.options[serviceSelect.selectedIndex];
                if (selectedOption.value) {
                    showRoomDetailsBtn.disabled = false;
                    let serviceData;
                    try {
                        serviceData = JSON.parse(selectedOption.getAttribute('data-service') || '{}');
                    } catch (error) {
                        console.error('Erreur lors du parsing de data-service:', error);
                        serviceData = {
                            nom: 'N/A',
                            salleStatut: 'N/A',
                            description: '{{ "Aucune description disponible"|trans|default("Aucune description disponible") }}'
                        };
                    }

                    // Débogage : Afficher les données du service
                    console.log('Service sélectionné:', serviceData);
                    console.log('Statut de la salle:', serviceData.salleStatut);

                    // Mettre à jour les éléments de la modale
                    document.getElementById('roomDetailsModalLabel').innerHTML = `<i class="bi bi-info-circle me-2"></i>{{ 'Détails de la salle pour'|trans|default('Détails de la salle pour') }} ${serviceData.nom || 'N/A'}`;

                    const salleStatutBadge = document.getElementById('modal-salle-statut');
                    const salleStatut = serviceData.salleStatut || 'N/A';
                    salleStatutBadge.textContent = salleStatut.charAt(0).toUpperCase() + salleStatut.slice(1);
                    salleStatutBadge.className = `status-badge ${salleStatut.toLowerCase() === 'ouvert' ? 'status-active' : 'status-inactive'}`;

                    document.getElementById('modal-description').textContent = serviceData.description || '{{ "Aucune description disponible"|trans|default("Aucune description disponible") }}';

                    // Afficher la section météo uniquement si un service est sélectionné et que la salle n'est pas fermée
                    if (salleStatut.toLowerCase() === 'fermé') {
                        console.log('Masquage de la section météo car la salle est fermée');
                        weatherCard.classList.add('d-none');
                    } else {
                        console.log('Affichage de la section météo car la salle n\'est pas fermée et un service est sélectionné');
                        weatherCard.classList.remove('d-none');
                        weatherCard.classList.add('animate__animated', 'animate__fadeInUp'); // Animation d'apparition
                    }
                } else {
                    showRoomDetailsBtn.disabled = true;
                    // Masquer la section météo si aucun service n'est sélectionné
                    console.log('Aucun service sélectionné, masquage de la section météo');
                    weatherCard.classList.add('d-none');
                }
            });

            showRoomDetailsBtn.addEventListener('click', (e) => {
                e.preventDefault();
                e.stopPropagation();
                console.log('Bouton "Détails de la salle" cliqué');
                try {
                    modal.show();
                } catch (error) {
                    console.error('Erreur lors de l\'ouverture de la modale:', error);
                }
            });

            // Débogage pour détecter les clics inattendus
            document.addEventListener('click', (e) => {
                if (e.target !== showRoomDetailsBtn) {
                    console.log('Clic détecté sur:', e.target);
                }
            });

            // Débogage des soumissions de formulaire
            document.querySelector('form').addEventListener('submit', (e) => {
                console.log('Formulaire soumis');
            });

            // Débogage des navigations
            window.addEventListener('beforeunload', (e) => {
                console.log('Navigation détectée');
            });
        });
    </script>
</body>
</html>