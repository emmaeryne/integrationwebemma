{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_equipe</th>
                <td>{{ equipe.idEquipe }}</td>
            </tr>
            <tr>
                <th>Nom_equipe</th>
                <td>{{ equipe.nomEquipe }}</td>
            </tr>
            <tr>
                <th>Type_equipe</th>
                <td>{{ equipe.typeEquipe }}</td>
            </tr>
            <tr>
                <th>Id_tournoi</th>
                <td>{{ tournoi.id_tournoi }}</td>
            </tr>
            <tr>
                <th>Nom_tournoi</th>
                <td>{{ tournoi.nom_tournoi }}</td>
            </tr>
            <tr>
                <th>Type_tournoi</th>
                <td>{{ tournoi.type_tournoi }}</td>
            </tr>
            <tr>
                <th>Date_tournoi</th>
                <td>{{ tournoi.date_tournoi ? tournoi.date_tournoi|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description_tournoi</th>
                <td>{{ tournoi.description_tournoi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipe_index') }}">back to list</a>

    <a href="{{ path('app_equipe_edit', {'id_equipe': equipe.id_equipe}) }}">edit</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
