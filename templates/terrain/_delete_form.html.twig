<form method="post" action="{{ path('app_terrain_delete', {'id_terrain': terrain.id_terrain}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terrain.id_terrain) }}">
    <button class="btn">Delete</button>
</form>
