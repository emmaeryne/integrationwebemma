<!DOCTYPE html>
<html lang="en" class="light-style layout-menu-fixed" dir="ltr" data-theme="theme-default" data-assets-path="{{ asset('sneat-1.0.0/') }}" data-template="vertical-menu-template-free">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0" />
    <title>Create New Carrier - Admin | Hive</title>

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="{{ asset('sneat-1.0.0/assets/img/favicon/favicon.ico') }}" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- Icons -->
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/fonts/boxicons.css') }}" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Core CSS -->
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/css/core.css') }}" class="template-customizer-core-css" />
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/css/theme-default.css') }}" class="template-customizer-theme-css" />
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/css/demo.css') }}" />

    <!-- Vendors CSS -->
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/libs/perfect-scrollbar/perfect-scrollbar.css') }}" />
    <link rel="stylesheet" href="{{ asset('sneat-1 جهت:ltr;0.0/assets/vendor/libs/apex-charts/apex-charts.css') }}" />

    <!-- Custom CSS -->
    <style>
        .carriers-wrapper {
            max-width: 1200px;
            margin: 40px auto;
            padding: 30px;
            background: #ffffff;
            border-radius: 15px;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.08);
            font-family: 'Poppins', sans-serif;
        }
        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 35px;
            border-bottom: 4px solid #3498db;
            padding-bottom: 15px;
        }
        .header-section h1 {
            font-size: 2.5rem;
            color: #2c3e50;
            margin: 0;
            font-weight: 700;
            letter-spacing: 0.5px;
            font-family: 'Public Sans', sans-serif;
        }
        .btn-primary {
            background-color: #28a745;
            border-color: #28a745;
            padding: 8px 16px;
            font-weight: 600;
            border-radius: 10px;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #218838;
            border-color: #218838;
            transform: translateY(-2px);
        }
        .btn-outline-secondary {
            border-radius: 10px;
            transition: all 0.3s ease;
        }
        .btn-outline-secondary:hover {
            transform: translateY(-2px);
        }
        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            background: #f8f9fa;
            margin-bottom: 1.5rem;
            transition: transform 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
        }
        .card-header {
            background-color: #3498db;
            color: #fff;
            padding: 1rem;
            border-radius: 10px 10px 0 0;
        }
        .card-body {
            padding: 1.5rem;
            background: #fff;
            border-radius: 0 0 10px 10px;
        }
        .carrier-form .form-control {
            border: 1px solid #e0e0e0;
            transition: all 0.3s;
            font-family: 'Poppins', sans-serif;
        }
        .carrier-form .form-control:focus {
            border-color: #3498db;
            box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.15);
        }
        .carrier-form .card {
            border: none;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }
        .carrier-form .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
        }
        .carrier-form .btn {
            font-family: 'Poppins', sans-serif;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        .carrier-form .btn-outline-secondary:hover {
            background-color: #f8f9fa;
            border-color: #dee2e6;
            transform: translateY(-2px);
        }
        .carrier-form .btn-primary:hover {
            background-color: #218838;
            border-color: #218838;
            transform: translateY(-2px);
        }
        .rounded-pill {
            border-radius: 50rem !important;
        }
        .form-floating > label {
            font-family: 'Public Sans', sans-serif;
            color: #6c757d;
        }
        .invalid-feedback {
            font-family: 'Poppins', sans-serif;
            font-size: 0.875rem;
        }
    </style>
</head>
<body>
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <!-- Menu -->
            <aside id="layout-menu" class="layout-menu menu-vertical menu bg-menu-theme">
                <div class="app-brand demo">
                    <a href="{{ path('app_dashboard') }}" class="app-brand-link">
                        <span class="app-brand-logo demo">
                            <img src="{{ asset('images/logo.png') }}" alt="Hive Logo" style="height: 80px; width: auto;" />
                        </span>
                        <span class="app-brand-text demo menu-text fw-bolder ms-2">HIVE</span>
                    </a>
                    <a href="javascript:void(0);" class="layout-menu-toggle menu-link text-large ms-auto d-block d-xl-none">
                        <i class="bx bx-chevron-left bx-sm align-middle"></i>
                    </a>
                </div>
                <ul class="menu-inner py-1">
                    <li class="menu-item">
                        <a href="{{ path('app_dashboard') }}" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-home-circle"></i>
                            <div data-i18n="Analytics">Dashboard</div>
                        </a>
                    </li>
                    <li class="menu-item active">
                        <a href="{{ path('admin_carrier_index') }}" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-truck"></i>
                            <div data-i18n="Carriers">Transporteurs</div>
                        </a>
                    </li>
                </ul>
            </aside>
            <!-- / Menu -->

            <!-- Layout container -->
            <div class="layout-page">
                <!-- Navbar -->
                <nav class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme" id="layout-navbar">
                    <!-- Navbar content can be added as needed -->
                </nav>
                <!-- / Navbar -->

                <!-- Content wrapper -->
                <div class="content-wrapper">
                    <div class="container-xxl flex-grow-1 container-p-y">
                        <div class="carriers-wrapper">
                            <!-- Header -->
                            <div class="header-section">
                                <h1><i class="bi bi-plus-circle me-2"></i>Create New Carrier</h1>
                                <a href="{{ path('admin_carrier_index') }}" class="btn btn-outline-secondary">
                                    <i class="bi bi-arrow-left me-1"></i> Back to list
                                </a>
                            </div>

                            <!-- Form Card -->
                            <div class="card shadow-sm border-0 rounded-lg overflow-hidden mb-4">
                                <div class="card-header" style="background-color: #3498db; color: #fff; padding: 1rem; border-radius: 10px 10px 0 0;">
                                    <h5 class="mb-0">
                                        <i class="bi bi-truck me-2"></i>
                                        New Carrier Form
                                    </h5>
                                </div>
                                <div class="card-body p-4" style="background: #fff; border-radius: 0 0 10px 10px;">
                                    {{ form_start(form, {'attr': {'class': 'needs-validation carrier-form', 'novalidate': 'novalidate'}}) }}
                                    <div class="row g-4">
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3">
                                                {{ form_widget(form.name, {
                                                    'attr': {
                                                        'class': 'form-control',
                                                        'placeholder': ' ',
                                                        'style': 'border-radius: 8px; border: 1px solid #e0e0e0;'
                                                    }
                                                }) }}
                                                {{ form_label(form.name, null, {'label_attr': {'class': 'form-label'}}) }}
                                                <div class="invalid-feedback">
                                                    Please provide a valid name.
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3">
                                                {{ form_widget(form.price, {
                                                    'attr': {
                                                        'class': 'form-control',
                                                        'placeholder': ' ',
                                                        'style': 'border-radius: 8px; border: 1px solid #e0e0e0;'
                                                    }
                                                }) }}
                                                {{ form_label(form.price) }}
                                                <div class="invalid-feedback">
                                                    Please provide a valid price.
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-12">
                                            <div class="form-floating mb-3">
                                                {{ form_widget(form.description, {
                                                    'attr': {
                                                        'class': 'form-control',
                                                        'placeholder': ' ',
                                                        'style': 'height: 120px; border-radius: 8px; border: 1px solid #e0e0e0;'
                                                    }
                                                }) }}
                                                {{ form_label(form.description) }}
                                            </div>
                                        </div>
                                    </div>
                                    <div class="card-footer bg-light d-flex justify-content-between py-3 px-4" style="border-radius: 0 0 10px 10px;">
                                        <a href="{{ path('admin_carrier_index') }}" class="btn btn-outline-secondary rounded-pill px-4" style="transition: all 0.3s ease; border-radius: 50rem;">
                                            <i class="bi bi-arrow-left me-1"></i> Back to list
                                        </a>
                                        <button type="submit" class="btn btn-primary rounded-pill px-4 shadow-sm" style="background-color: #28a745; border-color: #28a745; transition: all 0.3s ease; border-radius: 50rem;">
                                            <i class="bi bi-save me-1"></i> Save
                                        </button>
                                    </div>
                                    {{ form_end(form) }}
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Footer -->
                    <footer class="content-footer footer bg-footer-theme">
                        <div class="container-xxl d-flex flex-wrap justify-content-between py-2 flex-md-row flex-column">
                            <div class="mb-2 mb-md-0">
                                © {{ "now"|date("Y") }}, made with ❤️ by beeu
                            </div>
                        </div>
                    </footer>
                    <!-- / Footer -->
                </div>
                <!-- / Content wrapper -->
            </div>
            <!-- / Layout page -->
        </div>
        <div class="layout-overlay layout-menu-toggle"></div>
    </div>

    <!-- Core JS -->
    <script src="{{ asset('sneat-1.0.0/assets/vendor/libs/jquery/jquery.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/libs/popper/popper.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/js/bootstrap.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/libs/perfect-scrollbar/perfect-scrollbar.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/js/menu.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/js/main.js') }}"></script>
</body>
</html>