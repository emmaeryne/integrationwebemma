<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ 'Reservations'|trans }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="Fitness, Gym, HIVE, Training">
    <meta name="description" content="HIVE - Your ultimate fitness destination by BEEU">

    <!-- Favicon -->
    <link href="{{ asset('img/favicon.ico') }}" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="{{ asset('lib/flaticon/font/flaticon.css') }}" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="{{ asset('lib/owlcarousel/assets/owl.carousel.min.css') }}" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        :root {
            --bleu-vif: #00BFFF; /* Bright blue for highlights */
            --bleu: #1E90FF; /* Primary blue for buttons and accents */
            --bleu-fonce: #0A4D68; /* Dark blue for depth */
            --bleu-clair: #E6F0FA; /* Light blue matching background */
            --bleu-moyen: #4682B4; /* Mid-tone blue for balance */
            --gris: #2A2A2A; /* Dark gray for text */
            --jaune: #FFD700; /* Gold for standout elements */
            --shadow: 0 10px 30px rgba(30, 144, 255, 0.2); /* Subtle blue shadow */
            --shadow-dark: 0 8px 20px rgba(10, 77, 104, 0.3); /* Darker blue shadow */
        }
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2); /* Smooth gradient */
            color: var(--gris);
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }
        /* Header */
        .bg-dark {
            background: var(--bleu-fonce) !important;
            box-shadow: var(--shadow-dark);
            transition: background 0.3s ease;
        }
        .bg-secondary {
            background: var(--bleu-clair) !important;
            color: var(--bleu-fonce);
            border-bottom: 1px solid rgba(70, 130, 180, 0.2); /* Subtle mid-blue border */
        }
        .navbar-brand {
            display: flex;
            align-items: center;
            transition: transform 0.3s ease, filter 0.3s ease;
        }
        .navbar-brand:hover {
            transform: scale(1.05);
            filter: brightness(1.1);
        }
        .navbar-brand h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-weight: 700;
            font-size: 2.5rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 2px 2px 6px rgba(0, 191, 255, 0.4);
            margin: 0;
        }
        .navbar-brand img {
            max-height: 80px;
            margin-right: 15px;
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            transition: transform 0.3s ease;
        }
        .navbar-brand:hover img {
            transform: rotate(5deg);
        }
        .nav-link {
            color: var(--bleu-clair) !important;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            font-size: 1.1rem;
            padding: 10px 15px;
            transition: color 0.3s ease, background 0.3s ease;
        }
        .nav-link:hover, .nav-link.active {
            color: var(--bleu-vif) !important;
            background: rgba(0, 191, 255, 0.1); /* Subtle hover background */
            border-radius: 5px;
        }
        .dropdown-menu {
            background: var(--bleu-fonce);
            border: none;
            box-shadow: var(--shadow-dark);
        }
        .dropdown-item {
            color: var(--bleu-clair);
            font-weight: 400;
            transition: background 0.3s ease, color 0.3s ease;
        }
        .dropdown-item:hover {
            background: var(--bleu);
            color: #fff;
        }
        /* Nested Dropdown */
        .dropdown-menu .dropdown-menu {
            top: 0;
            left: 100%;
            margin-left: 0.1rem;
            background: var(--bleu-fonce);
            box-shadow: var(--shadow-dark);
        }
        .dropdown-menu .dropdown:hover > .dropdown-menu {
            display: block;
        }
        /* Buttons */
        .btn-primary {
            background: var(--bleu);
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-primary:hover {
            background: var(--bleu-vif);
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 191, 255, 0.4);
        }
        .btn-light {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-light:hover {
            background: var(--bleu-moyen);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(70, 130, 180, 0.3);
        }
        .btn-success {
            background: #28a745;
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-success:hover {
            background: #218838;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(40, 167, 69, 0.4);
        }
        .btn-warning {
            background: #f1c40f;
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-warning:hover {
            background: #e4b70d;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(241, 196, 15, 0.4);
        }
        .btn-danger {
            background: #dc3545;
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-danger:hover {
            background: #c82333;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(220, 53, 69, 0.4);
        }
        /* Social Media Icons */
        .btn-square {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            width: 40px;
            height: 40px;
            line-height: 40px;
            border-radius: 50%;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-square:hover {
            background: var(--bleu-vif);
            color: #fff;
            transform: scale(1.1);
            box-shadow: 0 8px 15px rgba(0, 191, 255, 0.4);
        }
        /* Contact Info */
        .bg-secondary h6 {
            font-family: 'Poppins', sans-serif;
            font-weight: 400;
            font-size: 0.95rem;
            margin: 0;
        }
        .bg-secondary i {
            color: var(--bleu-vif);
            font-size: 1.2rem;
            margin-right: 8px;
            transition: transform 0.3s ease;
        }
        .bg-secondary div:hover i {
            transform: scale(1.2);
        }
        /* Carousel */
        .carousel-caption {
            background: rgba(10, 77, 104, 0.75); /* Slightly more opaque */
            padding: 25px 50px;
            border-radius: 12px;
            box-shadow: var(--shadow-dark);
            animation: fadeInUp 1s ease;
        }
        .carousel-caption h5 {
            color: var(--jaune);
            font-family: 'Oswald', sans-serif;
            font-size: 1.6rem;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 15px;
        }
        .carousel-caption h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-size: 3.8rem;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 3px 3px 8px rgba(0, 191, 255, 0.5);
            margin-bottom: 25px;
        }
        .carousel-control-prev, .carousel-control-next {
            width: 5%;
            opacity: 0.8;
            transition: opacity 0.3s ease;
        }
        .carousel-control-prev:hover, .carousel-control-next:hover {
            opacity: 1;
        }
        .carousel-control-prev-icon, .carousel-control-next-icon {
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            width: 40px;
            height: 40px;
        }
        /* Table */
        .table {
            background: #fff;
            border-radius: 8px;
            box-shadow: var(--shadow);
        }
        .table-dark {
            background: var(--bleu-fonce);
        }
        .table-hover tbody tr:hover {
            background: rgba(0, 191, 255, 0.1);
        }
        .badge {
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            padding: 6px 12px;
            border-radius: 5px;
        }
        /* Alerts */
        .alert {
            border-radius: 8px;
            box-shadow: var(--shadow);
        }
        .alert-success {
            background: #d4edda;
            color: #155724;
        }
        .alert-danger {
            background: #f8d7da;
            color: #721c24;
        }
        /* Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(60px); }
            to { opacity: 1; transform: translateY(0); }
        }
        /* Responsive Adjustments */
        @media (max-width: 991px) {
            .navbar-brand h1 {
                font-size: 2rem;
            }
            .navbar-brand img {
                max-height: 60px;
            }
            .nav-link {
                font-size: 1rem;
                padding: 8px 12px;
            }
            .carousel-caption h1 {
                font-size: 2.5rem;
            }
            .carousel-caption h5 {
                font-size: 1.2rem;
            }
            .btn-primary, .btn-light, .btn-success, .btn-warning, .btn-danger {
                padding: 10px 20px;
                font-size: 0.9rem;
            }
            .dropdown-menu .dropdown-menu {
                left: 0;
                margin-left: 0;
            }
        }
        @media (max-width: 576px) {
            .carousel-caption {
                padding: 15px 25px;
            }
            .carousel-caption h1 {
                font-size: 1.8rem;
            }
            .carousel-caption h5 {
                font-size: 1rem;
            }
            .table-responsive {
                font-size: 0.9rem;
            }
            .btn-group .btn {
                font-size: 0.8rem;
                padding: 6px 12px;
            }
        }
    </style>
</head>
<body>
    <!-- Header Start -->
    <div class="container-fluid bg-dark px-0 fixed-top">
        <div class="row gx-0">
            <div class="col-lg-3 bg-dark d-none d-lg-block">
                <a href="{{ path('app_home1') }}" class="navbar-brand w-100 h-100 m-0 p-0 d-flex align-items-center justify-content-center">
                    <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                    <h1>HIVE</h1>
                </a>
            </div>
            <div class="col-lg-9">
                <div class="row gx-0 bg-secondary d-none d-lg-flex animate__animated animate__fadeInDown">
                    <div class="col-lg-7 px-5 text-start">
                        <div class="h-100 d-inline-flex align-items-center py-3 me-4">
                            <i class="fa fa-envelope"></i>
                            <h6>info@hivebybeeu.com</h6>
                        </div>
                        <div class="h-100 d-inline-flex align-items-center py-3">
                            <i class="fa fa-phone-alt"></i>
                            <h6>+21622820210</h6>
                        </div>
                    </div>
                    <div class="col-lg-5 px-5 text-end">
                        <div class="d-inline-flex align-items-center py-3">
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a class="btn btn-square rounded-circle animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-youtube"></i>
                            </a>
                        </div>
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg bg-dark navbar-dark p-3 p-lg-0 px-lg-5">
                    <a href="{{ path('app_home1') }}" class="navbar-brand d-block d-lg-none">
                        <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                        <h1>HIVE</h1>
                    </a>
                    <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                        <div class="navbar-nav mr-auto py-0">
                            <a href="{{ path('app_home_client') }}" class="nav-item nav-link active">Home</a>
                            <a href="{{ path('app_about1') }}" class="nav-item nav-link">About</a>
                                <a href="{{ path('app_produit_index') }}" class="nav-item nav-link">Liste Produits</a>
                            
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Reserver</a>
                                <div class="dropdown-menu rounded-0 m-0">
                                    <div class="dropdown">
                                        <a href="#" class="dropdown-item dropdown-toggle" data-bs-toggle="dropdown">Client</a>
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('client_services') }}" class="dropdown-item">Services Client</a>
                                            <a href="{{ path('app_client_reservation_index') }}" class="dropdown-item">Abonnement</a>
                                            <a href="{{ path('app_cours_liste_participant', {'user_id': app.user.id}) }}" class="dropdown-item">Participation</a>
                                        </div>
                                        
                                    </div>
                                   
                                </div>
                                
                            </div>

                        </div>
                        
                    </div>
                      <div class="navbar-nav ms-auto py-0">
                            {% if app.user %}
                                <div class="nav-item dropdown">
                                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                        <i class="fas fa-user-circle me-1"></i> {{ app.user.username }}
                                    </a>
                                    <div class="dropdown-menu dropdown-menu-end rounded-0 m-0">
                                        <a href="{{ path('app_home_client') }}" class="dropdown-item">Mon Profil</a>
                                        <a href="{{ path('app_logout') }}" class="dropdown-item text-danger">
                                        
                                            <i class="fas fa-sign-out-alt me-1"></i> Déconnexion
                                        </a>
                                    </div>
                                </div>
                            {% else %}
                                <a href="{{ path('app_login') }}" class="nav-item nav-link">Connexion</a>
                                <a href="{{ path('app_register') }}" class="nav-item nav-link">Inscription</a>
                            {% endif %}
                        </div>
                    </div>
                </nav>
            </div>
                </nav>
            </div>
        </div>
    </div>
              
                      
    <!-- Header End -->

    <!-- Carousel Start -->
    <div class="container-fluid p-0 mb-5">
        <div id="header-carousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="w-100" src="{{ asset('img/carousel-1.jpg') }}" alt="Build Your Strength" style="object-fit: cover; height: 100vh; filter: brightness(0.8) contrast(1.1);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="animate__animated animate__fadeIn">Best Fitness Experience</h5>
                            <h1 class="mb-md-4 animate__animated animate__zoomIn">Forge Your Body with HIVE</h1>
   
                        </div>
                    </div>
                </div>
                <div class="carousel-item">
                    <img class="w-100" src="{{ asset('img/carousel-2.jpg') }}" alt="Grow Stronger" style="object-fit: cover; height: 100vh; filter: brightness(0.8) contrast(1.1);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <div class="p-3" style="max-width: 900px;">
                            <h5 class="animate__animated animate__fadeIn">Unleash Your Potential</h5>
                            <h1 class="mb-md-4 animate__animated animate__zoomIn">Strength Begins at HIVE</h1>
                            <a href="#" class="btn btn-primary py-md-3 px-md-5 me-3 animate__animated animate__bounceIn">Join Now</a>
                            <a href="{{ path('app_contact1') }}" class="btn btn-light py-md-3 px-md-5 animate__animated animate__bounceIn">Contact Us</a>
                        </div>
                    </div>
                </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#header-carousel" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#header-carousel" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>
    <!-- Carousel End -->

    <!-- Reservations Section -->
    <div class="container mt-4">
        <h1 class="mb-4 animate__animated animate__fadeIn">{{ 'Reservations'|trans }}</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success alert-dismissible fade show animate__animated animate__fadeInUp" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger alert-dismissible fade show animate__animated animate__fadeInUp" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
        

        <div class="table-responsive card shadow-sm">
            <div class="card-body">
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col">{{ 'Abonnement'|trans }}</th>
                            <th scope="col">{{ 'Date de reservation'|trans }}</th>
                            <th scope="col">{{ 'Date de debut'|trans }}</th>
                            <th scope="col">{{ 'Date de fin'|trans }}</th>
                            <th scope="col">{{ 'status'|trans }}</th>
                            <th scope="col">{{ 'actions'|trans }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for reservation in reservations %}
                            <tr class="animate__animated animate__fadeInUp">
                                <td>{{ reservation.typeAbonnement ? reservation.typeAbonnement.nom : 'N/A' }}</td>
                                <td>{{ reservation.dateReservation ? reservation.dateReservation|date('d/m/Y H:i') : '-' }}</td>
                                <td>{{ reservation.dateDebut ? reservation.dateDebut|date('d/m/Y') : '-' }}</td>
                                <td>{{ reservation.dateFin ? reservation.dateFin|date('d/m/Y') : '-' }}</td>
                                <td>
                                    {% set statusClass = {
                                        'confirmed': 'success',
                                        'pending': 'warning',
                                        'cancelled': 'danger',
                                        'default': 'secondary'
                                    } %}
                                    <span class="badge bg-{{ statusClass[reservation.statut] ?? statusClass['default'] }}">
                                        {{ reservation.statut|trans }}
                                    </span>
                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        
                                            
                                        </a>
                                        <a href="{{ path('app_client_reservation_badge', {'id': reservation.id}) }}" class="btn btn-warning btn-sm me-2 shadow-sm" title="{{ 'download_badge'|trans }}">
                                            <i class="bi bi-download me-1"></i> {{ 'download_badge'|trans }}
                                        </a>
                                        <form method="post" action="{{ path('app_client_reservation_delete', {'id': reservation.id}) }}" style="display:inline;" onsubmit="return confirm('{{ 'delete_confirm'|trans|e('js') }}');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.id) }}">
                                            <button class="btn btn-danger btn-sm shadow-sm" title="{{ 'delete'|trans }}">
                                                <i class="bi bi-trash-fill me-1"></i> {{ 'delete'|trans }}
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="6" class="text-center py-4">
                                    <i class="fas fa-info-circle fa-2x mb-3"></i><br>
                                    {{ 'no_records'|trans }}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <a href="{{ path('app_client_reservation_new') }}" class="btn btn-success btn-lg mt-3 shadow animate__animated animate__bounceIn">
            <i class="bi bi-plus-circle-fill me-2"></i> {{ 'new'|trans }}
        </a>
    </div>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('lib/easing/easing.min.js') }}"></script>
    <script src="{{ asset('lib/waypoints/waypoints.min.js') }}"></script>
    <script src="{{ asset('lib/counterup/counterup.min.js') }}"></script>
    <script src="{{ asset('lib/owlcarousel/owl.carousel.min.js') }}"></script>

    <!-- Template Javascript -->
    <script src="{{ asset('js/main.js') }}"></script>

    <!-- Custom JavaScript -->
    <script>
        // Smooth scroll for nav links (optional enhancement)
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', function(e) {
                if (this.getAttribute('href').startsWith('#')) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });

        // Enable Bootstrap tooltips
        $(function () {
            $('[data-bs-toggle="tooltip"]').tooltip();
        });
    </script>
</body>
</html>