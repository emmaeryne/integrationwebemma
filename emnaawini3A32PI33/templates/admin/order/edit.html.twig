{% extends 'base.html.twig' %}

{% block title %}Modifier la commande #{{ order.id }}{% endblock %}

{% block content %}
<div class="card mb-4">
  <div class="card-header d-flex justify-content-between align-items-center">
    <h5 class="mb-0">Modifier la commande #{{ order.id }}</h5>
    <div>
      <a href="{{ path('admin_order_index') }}" class="btn btn-secondary">
        <i class="bx bx-arrow-back me-1"></i> Retour
      </a>
      {{ include('admin/order/_delete_form.html.twig') }}
    </div>
  </div>
  <div class="card-body">
    {{ include('admin/order/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
  </div>
</div>
{% endblock %}