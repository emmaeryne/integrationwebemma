<form method="post" action="{{ path('app_joueur_delete', {'id_joueur': joueur.id_joueur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ joueur.id_joueur) }}">
    <button class="btn">Delete</button>
</form>
