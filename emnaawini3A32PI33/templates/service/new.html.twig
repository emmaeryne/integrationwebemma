{% extends 'base.html.twig' %}

{% block title %}Nouveau service - Gestion | Hive{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/fonts/boxicons.css') }}" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/css/core.css') }}" class="template-customizer-core-css" />
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/css/theme-default.css') }}" class="template-customizer-theme-css" />
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/css/demo.css') }}" />
    <link rel="stylesheet" href="{{ asset('sneat-1.0.0/assets/vendor/libs/perfect-scrollbar/perfect-scrollbar.css') }}" />
    <style>
        .service-wrapper {
            max-width: 1200px;
            margin: 40px auto;
            padding: 30px;
            background: #ffffff;
            border-radius: 15px;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.08);
        }
        .header-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 35px;
            border-bottom: 4px solid #696cff;
            padding-bottom: 15px;
        }
        .header-section h1 {
            font-size: 2.5rem;
            color: #2c3e50;
            margin: 0;
            font-weight: 700;
            letter-spacing: 0.5px;
        }
        .form-control:focus, .form-select:focus {
            border-color: #696cff;
            box-shadow: 0 0 8px rgba(105, 108, 255, 0.3);
        }
        .form-label {
            font-weight: 500;
            color: #2c3e50;
        }
        .btn-primary {
            background-color: #696cff;
            border-color: #696cff;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #5a5cff;
            border-color: #5a5cff;
            transform: translateY(-2px);
        }
        .btn-secondary {
            transition: all 0.3s ease;
        }
        .btn-secondary:hover {
            transform: translateY(-2px);
        }
        /* Styles pour les erreurs */
        .error-message {
            color: #ff3e1d;
            font-size: 0.875rem;
            margin-bottom: 0.25rem;
            display: none;
        }
        .form-control.is-invalid, .form-select.is-invalid {
            border-color: #ff3e1d !important;
        }
        .form-group.has-error .error-message {
            display: block;
        }
        .form-group.has-error .form-control,
        .form-group.has-error .form-select {
            border-color: #ff3e1d !important;
        }
        .form-group.has-success .form-control,
        .form-group.has-success .form-select {
            border-color: #28a745 !important;
        }
        /* Icône de validation */
        .form-group.has-success::after {
            content: '\f00c'; /* FontAwesome check icon */
            font-family: 'Font Awesome 5 Free';
            font-weight: 900;
            color: #28a745;
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
        }
        .form-group.has-error::after {
            content: '\f06a'; /* FontAwesome exclamation icon */
            font-family: 'Font Awesome 5 Free';
            font-weight: 900;
            color: #ff3e1d;
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
        }
        .form-group {
            position: relative;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <!-- Menu -->
            <aside id="layout-menu" class="layout-menu menu-vertical menu bg-menu-theme">
                <div class="app-brand demo">
                    <a href="{{ path('app_dashboard') }}" class="app-brand-link">
                        <span class="app-brand-logo demo">
                            <img src="{{ asset('images/logo.png') }}" alt="Hive Logo" style="height: 80px; width: auto;" />
                        </span>
                        <span class="app-brand-text demo menu-text fw-bolder ms-2">HIVE</span>
                    </a>
                    <a href="javascript:void(0);" class="layout-menu-toggle menu-link text-large ms-auto d-block d-xl-none">
                        <i class="bx bx-chevron-left bx-sm align-middle"></i>
                    </a>
                </div>
                <div class="menu-inner-shadow"></div>
                <ul class="menu-inner py-1">
                    <li class="menu-item">
                        <a href="{{ path('app_dashboard') }}" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-home-circle"></i>
                            <div data-i18n="Analytics">Dashboard</div>
                        </a>
                    </li>
                    <li class="menu-item active">
                        <a href="{{ path('app_service_index') }}" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-cog"></i>
                            <div data-i18n="Services">Services</div>
                        </a>
                    </li>
                    <li class="menu-item">
                        <a href="{{ path('app_abonnement_index') }}" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-cog"></i>
                            <div data-i18n="Abonnements">Abonnements</div>
                        </a>
                    </li>
                    <li class="menu-item">
                        <a href="{{ path('app_type_abonnement_index') }}" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-cog"></i>
                            <div data-i18n="Association">Types d'abonnement</div>
                        </a>
                    </li>
                    <li class="menu-item">
                        <a href="javascript:void(0);" class="menu-link menu-toggle">
                            <i class="menu-icon tf-icons bx bx-layout"></i>
                            <div data-i18n="Layouts">Layouts</div>
                        </a>
                        <ul class="menu-sub">
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Without menu">Without menu</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Without navbar">Without navbar</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Container">Container</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Fluid">Fluid</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Blank">Blank</div></a></li>
                        </ul>
                    </li>
                    <li class="menu-header small text-uppercase"><span class="menu-header-text">Pages</span></li>
                    <li class="menu-item">
                        <a href="javascript:void(0);" class="menu-link menu-toggle">
                            <i class="menu-icon tf-icons bx bx-dock-top"></i>
                            <div data-i18n="Account Settings">Account Settings</div>
                        </a>
                        <ul class="menu-sub">
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Account">Account</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Notifications">Notifications</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Connections">Connections</div></a></li>
                        </ul>
                    </li>
                    <li class="menu-item">
                        <a href="javascript:void(0);" class="menu-link menu-toggle">
                            <i class="menu-icon tf-icons bx bx-lock-open-alt"></i>
                            <div data-i18n="Authentications">Authentications</div>
                        </a>
                        <ul class="menu-sub">
                            <li class="menu-item"><a href="#" class="menu-link" target="_blank"><div data-i18n="Basic">Login</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link" target="_blank"><div data-i18n="Basic">Register</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link" target="_blank"><div data-i18n="Basic">Forgot Password</div></a></li>
                        </ul>
                    </li>
                    <li class="menu-item">
                        <a href="javascript:void(0);" class="menu-link menu-toggle">
                            <i class="menu-icon tf-icons bx bx-cube-alt"></i>
                            <div data-i18n="Misc">Misc</div>
                        </ blessing>
                        <ul class="menu-sub">
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Error">Error</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Under Maintenance">Under Maintenance</div></a></li>
                        </ul>
                    </li>
                    <li class="menu-header small text-uppercase"><span class="menu-header-text">Components</span></li>
                    <li class="menu-item">
                        <a href="#" class="menu-link">
                            <i class="menu-icon tf-icons bx bx-collection"></i>
                            <div data-i18n="Basic">Cards</div>
                        </a>
                    </li>
                    <li class="menu-item">
                        <a href="javascript:void(0)" class="menu-link menu-toggle">
                            <i class="menu-icon tf-icons bx bx-box"></i>
                            <div data-i18n="User interface">User interface</div>
                        </a>
                        <ul class="menu-sub">
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Accordion">Accordion</div></a></li>
                            <li class="menu-item"><a href="#" class="menu-link"><div data-i18n="Alerts">Alerts</div></a></li>
                        </ul>
                    </li>
                </ul>
            </aside>
            <!-- / Menu -->

            <!-- Contenu principal -->
            <div class="layout-page">
                <!-- Navbar -->
                <nav class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme" id="layout-navbar">
                    <div class="layout-menu-toggle navbar-nav align-items-xl-center me-3 me-xl-0 d-xl-none">
                        <a class="nav-item nav-link px-0 me-xl-4" href="javascript:void(0)">
                            <i class="bx bx-menu bx-sm"></i>
                        </a>
                    </div>
                    <div class="navbar-nav-right d-flex align-items-center" id="navbar-collapse">
                        <div class="navbar-nav align-items-center">
                            <div class="nav-item d-flex align-items-center">
                                <i class="bx bx-search fs-4 lh-0"></i>
                                <input type="text" class="form-control border-0 shadow-none" placeholder="Search..." aria-label="Search..." />
                            </div>
                        </div>
                        <ul class="navbar-nav flex-row align-items-center ms-auto">
                            <li class="nav-item lh-1 me-3">
                                <a class="github-button" href="https://github.com/themeselection/sneat-html-admin-template-free" data-icon="octicon-star" data-size="large" data-show-count="true" aria-label="Star themeselection/sneat-html-admin-template-free on GitHub">Star</a>
                            </li>
                            <li class="nav-item navbar-dropdown dropdown-user dropdown">
                                <a class="nav-link dropdown-toggle hide-arrow" href="javascript:void(0);" data-bs-toggle="dropdown">
                                    <div class="avatar avatar-online">
                                        <img src="{{ asset('sneat-1.0.0/assets/img/avatars/1.png') }}" alt class="w-px-30 h-auto rounded-circle" />
                                    </div>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" href="#">
                                            <div class="d-flex">
                                                <div class="flex-shrink-0 me-3">
                                                    <div class="avatar avatar-online">
                                                        <img src="{{ asset('sneat-1.0.0/assets/img/avatars/1.png') }}" alt class="w-px-30 h-auto rounded-circle" />
                                                    </div>
                                                </div>
                                                <div class="flex-grow-1">
                                                    <span class="fw-semibold d-block">John Doe</span>
                                                    <small class="text-muted">Admin</small>
                                                </div>
                                            </div>
                                        </a>
                                    </li>
                                    <li><div class="dropdown-divider"></div></li>
                                    <li><a class="dropdown-item" href="#"><i class="bx bx-user me-2"></i> <span class="align-middle">My Profile</span></a></li>
                                    <li><a class="dropdown-item" href="#"><i class="bx bx-cog me-2"></i> <span class="align-middle">Settings</span></a></li>
                                    <li><div class="dropdown-divider"></div></li>
                                    <li><a class="dropdown-item" href="#"><i class="bx bx-power-off me-2"></i> <span class="align-middle">Log Out</span></a></li>
                                </ul>
                            </li>
                        </ul>
                    </div>
                </nav>
                <!-- / Navbar -->

                <!-- Contenu -->
                <div class="content-wrapper">
                    <div class="container-xxl flex-grow-1 container-p-y">
                        <div class="service-wrapper">
                            <!-- En-tête -->
                            <div class="header-section">
                                <h1><i class="bi bi-plus-circle me-2"></i>Ajouter un nouveau service</h1>
                            </div>

                            <!-- Messages flash -->
                            {% for label, messages in app.flashes %}
                                {% for message in messages %}
                                    <div class="alert alert-{{ label == 'success' ? 'success' : 'danger' }} alert-dismissible fade show" role="alert">
                                        <i class="bi {{ label == 'success' ? 'bi-check-circle' : 'bi-exclamation-triangle' }} me-2"></i> {{ message }}
                                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                    </div>
                                {% endfor %}
                            {% endfor %}

                            <!-- Formulaire -->
                            {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
                                <div class="row g-4">
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.nom, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.nom) }}
                                            </div>
                                            {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.prix, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.prix) }}
                                            </div>
                                            {{ form_widget(form.prix, {'attr': {'class': 'form-control', 'step': '0.01'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.categorie, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.categorie) }}
                                            </div>
                                            {{ form_widget(form.categorie, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.capaciteMax, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.capaciteMax) }}
                                            </div>
                                            {{ form_widget(form.capaciteMax, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.dureeMinutes, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.dureeMinutes) }}
                                            </div>
                                            {{ form_widget(form.dureeMinutes, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.niveau, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.niveau) }}
                                            </div>
                                            {{ form_widget(form.niveau, {'attr': {'class': 'form-select'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.salle, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.salle) }}
                                            </div>
                                            {{ form_widget(form.salle, {'attr': {'class': 'form-select'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.description, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.description) }}
                                            </div>
                                            {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': '4'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.estActif, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.estActif) }}
                                            </div>
                                            {{ form_widget(form.estActif, {'attr': {'class': 'form-select'}}) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3 form-group">
                                            {{ form_label(form.image, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="error-message">
                                                <i class="bi bi-exclamation-circle me-1"></i>
                                                {{ form_errors(form.image) }}
                                            </div>
                                            {{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
                                        </div>
                                    </div>
                                </div>

                                <div class="d-flex justify-content-between mt-4">
                                    <button type="submit" class="btn btn-primary">
                                        <i class="bi bi-save me-2"></i>Enregistrer
                                    </button>
                                    <a href="{{ path('app_service_index') }}" class="btn btn-secondary">
                                        <i class="bi bi-arrow-left me-2"></i>Retour à la liste
                                    </a>
                                </div>
                            {{ form_end(form) }}
                        </div>
                    </div>

                    <!-- Footer -->
                    <footer class="content-footer footer bg-footer-theme">
                        <div class="container-xxl d-flex flex-wrap justify-content-between py-2 flex-md-row flex-column">
                            <div class="mb-2 mb-md-0">
                                © {{ "now"|date("Y") }}, made with ❤️ by beeu
                            </div>
                        </div>
                    </footer>
                </div>
            </div>
        </div>
        <div class="layout-overlay layout-menu-toggle"></div>
    </div>

    <!-- Bouton "Upgrade to Pro" -->
    <div class="buy-now">
        <a href="https://themeselection.com/products/sneat-bootstrap-html-admin-template/" target="_blank" class="btn btn-danger btn-buy-now">Upgrade to Pro</a>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('sneat-1.0.0/assets/vendor/libs/jquery/jquery.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/libs/popper/popper.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/js/bootstrap.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/libs/perfect-scrollbar/perfect-scrollbar.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/vendor/js/menu.js') }}"></script>
    <script src="{{ asset('sneat-1.0.0/assets/js/main.js') }}"></script>
    <script async defer src="https://buttons.github.io/buttons.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Gestion des alertes temporisées
            setTimeout(() => {
                document.querySelectorAll('.alert').forEach(alert => alert.classList.remove('show'));
            }, 5000);

            // Ajouter dynamiquement les classes has-error et has-success
            const form = document.querySelector('.needs-validation');
            const formGroups = form.querySelectorAll('.form-group');

            const nomInput = form.querySelector('#service_nom');
            const prixInput = form.querySelector('#service_prix');
            const categorieInput = form.querySelector('#service_categorie');
            const capaciteInput = form.querySelector('#service_capaciteMax');
            const dureeInput = form.querySelector('#service_dureeMinutes');
            const niveauInput = form.querySelector('#service_niveau');
            const estActifInput = form.querySelector('#service_estActif');
            const salleInput = form.querySelector('#service_salle');

            // Validation en temps réel pour le champ "nom"
            nomInput.addEventListener('input', function() {
                const regex = /^[a-zA-ZÀ-ÿ0-9\s]+$/;
                const group = nomInput.closest('.form-group');
                const errorMessage = group.querySelector('.error-message');
                if (!regex.test(nomInput.value) && nomInput.value !== '') {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    nomInput.classList.add('is-invalid');
                    errorMessage.textContent = 'Le nom du service ne doit contenir que des lettres, des chiffres et des espaces.';
                } else if (nomInput.value.length < 3 && nomInput.value !== '') {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    nomInput.classList.add('is-invalid');
                    errorMessage.textContent = 'Le nom doit contenir au moins 3 caractères.';
                } else if (nomInput.value !== '') {
                    group.classList.remove('has-error');
                    group.classList.add('has-success');
                    nomInput.classList.remove('is-invalid');
                    nomInput.classList.add('is-valid');
                    errorMessage.textContent = '';
                }
            });

            // Validation en temps réel pour le champ "prix"
            prixInput.addEventListener('input', function() {
                const group = prixInput.closest('.form-group');
                const errorMessage = group.querySelector('.error-message');
                if (prixInput.value <= 0) {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    prixInput.classList.add('is-invalid');
                    errorMessage.textContent = 'Le prix doit être positif.';
                } else if (prixInput.value !== '') {
                    group.classList.remove('has-error');
                    group.classList.add('has-success');
                    prixInput.classList.remove('is-invalid');
                    prixInput.classList.add('is-valid');
                    errorMessage.textContent = '';
                }
            });

            // Validation en temps réel pour le champ "capaciteMax"
            capaciteInput.addEventListener('input', function() {
                const group = capaciteInput.closest('.form-group');
                const errorMessage = group.querySelector('.error-message');
                if (capaciteInput.value === '') {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    capaciteInput.classList.add('is-invalid');
                    errorMessage.textContent = 'La capacité maximale est obligatoire.';
                } else if (capaciteInput.value <= 0) {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    capaciteInput.classList.add('is-invalid');
                    errorMessage.textContent = 'La capacité doit être positive.';
                } else if (capaciteInput.value < 8 || capaciteInput.value > 60) {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    capaciteInput.classList.add('is-invalid');
                    errorMessage.textContent = 'La capacité doit être comprise entre 8 et 60 personnes.';
                } else {
                    group.classList.remove('has-error');
                    group.classList.add('has-success');
                    capaciteInput.classList.remove('is-invalid');
                    capaciteInput.classList.add('is-valid');
                    errorMessage.textContent = '';
                }
            });

            // Validation en temps réel pour le champ "dureeMinutes"
            dureeInput.addEventListener('input', function() {
                const group = dureeInput.closest('.form-group');
                const errorMessage = group.querySelector('.error-message');
                if (dureeInput.value === '') {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    dureeInput.classList.add('is-invalid');
                    errorMessage.textContent = 'La durée est obligatoire.';
                } else if (dureeInput.value <= 0) {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    dureeInput.classList.add('is-invalid');
                    errorMessage.textContent = 'La durée doit être positive.';
                } else if (dureeInput.value < 15 || dureeInput.value > 240) {
                    group.classList.add('has-error');
                    group.classList.remove('has-success');
                    dureeInput.classList.add('is-invalid');
                    errorMessage.textContent = 'La durée doit être comprise entre 15 et 240 minutes.';
                } else {
                    group.classList.remove('has-error');
                    group.classList.add('has-success');
                    dureeInput.classList.remove('is-invalid');
                    dureeInput.classList.add('is-valid');
                    errorMessage.textContent = '';
                }
            });

            // Validation en temps réel pour les champs obligatoires restants
            [categorieInput, niveauInput, estActifInput, salleInput].forEach(input => {
                input.addEventListener('input', function() {
                    const group = input.closest('.form-group');
                    const errorMessage = group.querySelector('.error-message');
                    if (input.value === '') {
                        group.classList.add('has-error');
                        group.classList.remove('has-success');
                        input.classList.add('is-invalid');
                        errorMessage.textContent = input.id === 'service_categorie' ? 'La catégorie est obligatoire.' :
                                                  input.id === 'service_niveau' ? 'Le niveau de difficulté est obligatoire.' :
                                                  input.id === 'service_estActif' ? 'Veuillez indiquer si le service est actif.' :
                                                  'L\'état de la salle est obligatoire.';
                    } else {
                        group.classList.remove('has-error');
                        group.classList.add('has-success');
                        input.classList.remove('is-invalid');
                        input.classList.add('is-valid');
                        errorMessage.textContent = '';
                    }
                });
            });

            // Vérifier les erreurs après la soumission
            form.addEventListener('submit', function(event) {
                let hasErrors = false;

                formGroups.forEach(group => {
                    const input = group.querySelector('.form-control, .form-select');
                    const errorMessage = group.querySelector('.error-message');

                    // Si le champ a une erreur (Symfony ajoute un message d'erreur)
                    if (errorMessage && errorMessage.textContent.trim() !== '') {
                        group.classList.add('has-error');
                        group.classList.remove('has-success');
                        input.classList.add('is-invalid');
                        hasErrors = true;
                    } else if (input.value !== '') {
                        // Si le champ est valide et non vide
                        group.classList.add('has-success');
                        group.classList.remove('has-error');
                        input.classList.remove('is-invalid');
                        input.classList.add('is-valid');
                    }
                });

                if (hasErrors) {
                    event.preventDefault();
                    event.stopPropagation();
                }

                form.classList.add('was-validated');
            });

            // Vérifier les erreurs déjà présentes au chargement de la page (après une soumission invalide)
            formGroups.forEach(group => {
                const errorMessage = group.querySelector('.error-message');
                const input = group.querySelector('.form-control, .form-select');

                if (errorMessage && errorMessage.textContent.trim() !== '') {
                    group.classList.add('has-error');
                    input.classList.add('is-invalid');
                } else if (input.value !== '') {
                    group.classList.add('has-success');
                    input.classList.add('is-valid');
                }
            });
        });
    </script>
{% endblock %}