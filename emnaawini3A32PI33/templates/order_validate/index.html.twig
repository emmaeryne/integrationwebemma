<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation de commande - HIVE</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="Fitness, Gym, HIVE, Training">
    <meta name="description" content="HIVE - Your ultimate fitness destination by BEEU">

    <!-- Favicon -->
    <link href="{{ asset('img/favicon.ico') }}" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="{{ asset('lib/flaticon/font/flaticon.css') }}" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="{{ asset('lib/owlcarousel/assets/owl.carousel.min.css') }}" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        :root {
            --bleu-vif: #00BFFF;
            --bleu: #1E90FF;
            --bleu-fonce: #0A4D68;
            --bleu-clair: #E6F0FA;
            --bleu-moyen: #4682B4;
            --gris: #2A2A2A;
            --jaune: #FFD700;
            --shadow: 0 10px 30px rgba(30, 144, 255, 0.2);
            --shadow-dark: 0 8px 20px rgba(10, 77, 104, 0.3);
        }
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
            color: #2c3e50;
            margin: 0;
            padding: 0;
        }
        .bg-dark {
            background: var(--bleu-fonce) !important;
            box-shadow: var(--shadow-dark);
            transition: background 0.3s ease;
        }
        .bg-secondary {
            background: var(--bleu-clair) !important;
            color: var(--bleu-fonce);
            border-bottom: 1px solid rgba(70, 130, 180, 0.2);
        }
        .navbar-brand {
            display: flex;
            align-items: center;
            transition: transform 0.3s ease, filter 0.3s ease;
        }
        .navbar-brand:hover {
            transform: scale(1.05);
            filter: brightness(1.1);
        }
        .navbar-brand h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-weight: 700;
            font-size: 2.5rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 2px 2px 6px rgba(0, 191, 255, 0.4);
            margin: 0;
        }
        .navbar-brand img {
            max-height: 80px;
            margin-right: 15px;
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            transition: transform 0.3s ease;
        }
        .navbar-brand:hover img {
            transform: rotate(5deg);
        }
        .nav-link {
            color: var(--bleu-clair) !important;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            font-size: 1.1rem;
            padding: 10px 15px;
            transition: color 0.3s ease, background 0.3s ease;
        }
        .nav-link:hover, .nav-link.active {
            color: var(--bleu-vif) !important;
            background: rgba(0, 191, 255, 0.1);
            border-radius: 5px;
        }
        .dropdown-menu {
            background: var(--bleu-fonce);
            border: none;
            box-shadow: var(--shadow-dark);
        }
        .dropdown-item {
            color: var(--bleu-clair);
            font-weight: 400;
            transition: background 0.3s ease, color 0.3s ease;
        }
        .dropdown-item:hover {
            background: var(--bleu);
            color: #fff;
        }
        .dropdown-menu .dropdown-menu {
            top: 0;
            left: 100%;
            margin-left: 0.1rem;
            background: var(--bleu-fonce);
            box-shadow: var(--shadow-dark);
        }
        .dropdown-menu .dropdown:hover > .dropdown-menu {
            display: block;
        }
        .confirmation-container {
            max-width: 900px;
            margin: 100px auto 40px;
            padding: 20px;
        }
        .confirmation-card {
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
            overflow: hidden;
        }
        .confirmation-header {
            background: var(--bleu-clair);
            padding: 40px;
            text-align: center;
        }
        .confirmation-header h1 {
            color: var(--bleu-fonce);
            margin: 20px 0 10px;
            font-size: 28px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
        }
        .reference {
            color: var(--bleu-moyen);
            font-size: 16px;
        }
        .confirmation-body {
            padding: 30px;
        }
        .confirmation-section, .delivery-info, .next-steps {
            margin-bottom: 30px;
        }
        .confirmation-section p, .delivery-info p, .next-steps li {
            font-size: 16px;
            line-height: 1.7;
            color: var(--gris);
        }
        .delivery-info h2, .next-steps h2 {
            font-size: 22px;
            margin-bottom: 15px;
            color: var(--bleu);
            display: flex;
            align-items: center;
            font-family: 'Oswald', sans-serif;
            font-weight: 500;
        }
        .delivery-info h2 i, .next-steps h2 i {
            margin-right: 10px;
            color: var(--bleu-vif);
        }
        .address-box {
            background: var(--bleu-clair);
            padding: 15px;
            border-radius: 5px;
            border-left: 4px solid var(--bleu);
            margin-top: 10px;
            color: var(--bleu-fonce);
            line-height: 1.6;
        }
        .next-steps ul {
            padding-left: 20px;
        }
        .confirmation-footer {
            background: var(--bleu-clair);
            padding: 20px 30px;
            display: flex;
            justify-content: center;
        }
        .account-btn {
            background: var(--bleu);
            color: white;
            padding: 12px 30px;
            border-radius: 5px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 16px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .account-btn:hover {
            background: var(--bleu-vif);
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 191, 255, 0.4);
            text-decoration: none;
        }
        .account-btn i {
            margin-right: 8px;
        }
        .footer {
            background: var(--bleu-fonce);
            color: var(--bleu-clair);
            padding: 3rem 0;
        }
        .footer h5 {
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            color: var(--bleu-vif);
            margin-bottom: 1.5rem;
        }
        .footer a {
            color: var(--bleu-clair);
            transition: color 0.3s ease;
        }
        .footer a:hover {
            color: var(--bleu-vif);
        }
        .footer .btn-square {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            width: 40px;
            height: 40px;
            line-height: 40px;
            border-radius: 50%;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .footer .btn-square:hover {
            background: var(--bleu-vif);
            color: #fff;
            transform: scale(1.1);
        }
        @media (max-width: 991px) {
            .navbar-brand h1 {
                font-size: 2rem;
            }
            .navbar-brand img {
                max-height: 60px;
            }
            .nav-link {
                font-size: 1rem;
                padding: 8px 12px;
            }
            .confirmation-container {
                margin: 80px auto 20px;
                padding: 15px;
            }
            .confirmation-header h1 {
                font-size: 24px;
            }
            .delivery-info h2, .next-steps h2 {
                font-size: 20px;
            }
        }
        @media (max-width: 576px) {
            .confirmation-container {
                margin: 60px auto 20px;
                padding: 10px;
            }
            .confirmation-header {
                padding: 20px;
            }
            .confirmation-header h1 {
                font-size: 20px;
            }
            .confirmation-section p, .delivery-info p, .next-steps li {
                font-size: 14px;
            }
            .delivery-info h2, .next-steps h2 {
                font-size: 18px;
            }
            .account-btn {
                padding: 10px 20px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <!-- Header Start -->
    <div class="container-fluid bg-dark px-0 fixed-top">
        <div class="row gx-0">
            <div class="col-lg-3 bg-dark d-none d-lg-block">
                <a href="{{ path('app_home1') }}" class="navbar-brand w-100 h-100 m-0 p-0 d-flex align-items-center justify-content-center">
                    <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                    <h1>HIVE</h1>
                </a>
            </div>
            <div class="col-lg-9">
                <div class="row gx-0 bg-secondary d-none d-lg-flex animate__animated animate__fadeInDown">
                    <div class="col-lg-7 px-5 text-start">
                        <div class="h-100 d-inline-flex align-items-center py-3 me-4">
                            <i class="fa fa-envelope"></i>
                            <h6>info@hivebybeeu.com</h6>
                        </div>
                        <div class="h-100 d-inline-flex align-items-center py-3">
                            <i class="fa fa-phone-alt"></i>
                            <h6>+21622820210</h6>
                        </div>
                    </div>
                    <div class="col-lg-5 px-5 text-end">
                        <div class="d-inline-flex align-items-center py-3">
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a class="btn btn-square rounded-circle animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-youtube"></i>
                            </a>
                        </div>
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg bg-dark navbar-dark p-3 p-lg-0 px-lg-5">
                    <a href="{{ path('app_home1') }}" class="navbar-brand d-block d-lg-none">
                        <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                        <h1>HIVE</h1>
                    </a>
                    <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                        <div class="navbar-nav mr-auto py-0">
                            <a href="{{ path('app_home1') }}" class="nav-item nav-link">Home</a>
                            <a href="{{ path('app_about1') }}" class="nav-item nav-link">About</a>
                            <a href="{{ path('app_produit_index') }}" class="nav-item nav-link">Liste Produits</a>
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Reserver</a>
                                <div class="dropdown-menu rounded-0 m-0">
                                    <div class="dropdown">
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('client_services') }}" class="dropdown-item">Services Client</a>
                                            <a href="{{ path('app_client_reservation_index') }}" class="dropdown-item">Abonnement</a>
                                        </div>
                                    </div>
                                    <div class="dropdown">
                                        <a href="#" class="dropdown-item dropdown-toggle" data-bs-toggle="dropdown">Coach</a>
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('app_coach_reservations') }}" class="dropdown-item">Espace coach</a>
                                            <a href="{{ path('app_planning_index', {'coachId': 2}) }}" class="dropdown-item">Planning</a>
                                            <a href="{{ path('app_cours_index', {'user_id': 2}) }}" class="dropdown-item">Cours</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
    <!-- Header End -->

    <!-- Content Start -->
    <div class="confirmation-container">
        <div class="confirmation-card">
            <!-- HEADER -->
            <div class="confirmation-header">
                <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="var(--bleu-vif)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                    <polyline points="22 4 12 14.01 9 11.01"></polyline>
                </svg>
                <h1>Commande Confirmée !</h1>
                <p class="reference">Référence : <strong>{{ order.reference }}</strong></p>
            </div>

            <!-- BODY -->
            <div class="confirmation-body">
                <div class="confirmation-section">
                    <p>Merci pour votre commande ! Nous avons bien reçu votre paiement.</p>
                    <p>Un email de confirmation a été envoyé à : <strong>{{ order.user.email }}</strong></p>
                </div>

                <div class="delivery-info">
                    <h2><i class="fas fa-truck"></i> Livraison</h2>
                    <p>Transporteur : <strong>{{ order.carrierName }}</strong></p>
                    <div class="address-box">
                        {{ order.delivery|raw }}
                    </div>
                </div>

                <div class="next-steps">
                    <h2><i class="fas fa-arrow-right"></i> Prochaines étapes</h2>
                    <ul>
                        <li>Vous recevrez un email dès l'expédition de votre commande.</li>
                        <li>Suivi disponible dans votre espace client.</li>
                    </ul>
                </div>
            </div>

            <!-- FOOTER -->
            <div class="confirmation-footer">
                <a href="{{ path('app_account_order') }}" class="account-btn">
                    <i class="fas fa-user-circle"></i> Voir mes commandes
                </a>
            </div>
        </div>
    </div>
    <!-- Content End -->

    <!-- Footer Start -->
    <div class="container-fluid footer">
        <div class="container py-5">
            <div class="row g-5">
                <div class="col-lg-4 col-md-6">
                    <h5 class="text-uppercase mb-4">À propos de HIVE</h5>
                    <p>HIVE by BEEU est votre destination ultime pour le fitness. Rejoignez-nous pour transformer votre corps et votre esprit avec nos programmes d'entraînement personnalisés.</p>
                    <div class="d-flex">
                        <a class="btn btn-square rounded-circle me-2" href="#"><i class="fab fa-facebook-f"></i></a>
                        <a class="btn btn-square rounded-circle me-2" href="#"><i class="fab fa-twitter"></i></a>
                        <a class="btn btn-square rounded-circle me-2" href="#"><i class="fab fa-instagram"></i></a>
                        <a class="btn btn-square rounded-circle" href="#"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div class="col-lg-4 col-md-6">
                    <h5 class="text-uppercase mb-4">Liens rapides</h5>
                    <a class="d-block mb-2" href="{{ path('app_home1') }}">Accueil</a>
                    <a class="d-block mb-2" href="{{ path('app_about1') }}">À propos</a>
                    <a class="d-block mb-2" href="{{ path('app_produit_index') }}">Produits</a>
                    <a class="d-block mb-2" href="{{ path('app_contact1') }}">Contact</a>
                </div>
                <div class="col-lg-4 col-md-6">
                    <h5 class="text-uppercase mb-4">Contactez-nous</h5>
                    <p><i class="fa fa-envelope me-2"></i>info@hivebybeeu.com</p>
                    <p><i class="fa fa-phone-alt me-2"></i>+21622820210</p>
                    <p><i class="fa fa-map-marker-alt me-2"></i>Tunis, Tunisie</p>
                </div>
            </div>
            <div class="text-center pt-4">
                <p class="mb-0">© {{ 'now'|date('Y') }} HIVE by BEEU. Tous droits réservés.</p>
            </div>
        </div>
    </div>
    <!-- Footer End -->

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('lib/easing/easing.min.js') }}"></script>
    <script src="{{ asset('lib/waypoints/waypoints.min.js') }}"></script>
    <script src="{{ asset('lib/counterup/counterup.min.js') }}"></script>
    <script src="{{ asset('lib/owlcarousel/owl.carousel.min.js') }}"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- Template JavaScript -->
    <script src="{{ asset('js/main.js') }}"></script>

    <!-- Custom JavaScript -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', function(e) {
                    if (this.getAttribute('href').startsWith('#')) {
                        e.preventDefault();
                        const target = document.querySelector(this.getAttribute('href'));
                        target.scrollIntoView({ behavior: 'smooth' });
                    }
                });
            });
        });
    </script>
</body>
</html>