{% extends 'base-front.html.twig' %}

{% block title %}Confirmation de commande{% endblock %}

{% block content %}
<div class="confirmation-container">
    <div class="confirmation-card">
        <!-- Bandeau supérieur -->
        <div class="confirmation-header">
            <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="#4CAF50" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                <polyline points="22 4 12 14.01 9 11.01"></polyline>
            </svg>
            <h1>Commande confirmée !</h1>
            <p class="reference">Référence : <strong>{{ order.reference }}</strong></p>
        </div>

        <!-- Corps du message -->
        <div class="confirmation-body">
            <p class="greeting">Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong>,</p>
            
            <div class="confirmation-section">
                <p>Nous avons bien reçu votre commande et nous vous en remercions.</p>
                <p>Un email de confirmation a été envoyé à : <strong>{{ order.user.email }}</strong></p>
            </div>

            <div class="delivery-info">
                <h2><i class="fas fa-truck"></i> Livraison</h2>
                <p>Transporteur : <strong>{{ order.carrierName }}</strong></p>
                <div class="address-box">
                    {{ order.delivery|raw }}
                </div>
            </div>

            <div class="next-steps">
                <h2><i class="fas fa-arrow-right"></i> Prochaines étapes</h2>
                <ul>
                    <li>Vous recevrez un email lorsque votre commande sera expédiée</li>
                    <li>Numéro de suivi disponible dans votre espace client</li>
                </ul>
            </div>
        </div>

        <!-- Pied de page -->
        <div class="confirmation-footer">
            <a href="{{ path('app_account_order') }}" class="account-btn">
                <i class="fas fa-user-circle"></i> Voir mes commandes
            </a>
           
        </div>
    </div>
</div>

<style>
.confirmation-container {
    max-width: 800px;
    margin: 40px auto;
    padding: 0 20px;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.confirmation-card {
    background: white;
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    overflow: hidden;
}

.confirmation-header {
    background: #f8f9fa;
    padding: 30px;
    text-align: center;
    border-bottom: 1px solid #eaeaea;
}

.confirmation-header h1 {
    color: #2c3e50;
    margin: 15px 0 5px;
    font-size: 28px;
}

.reference {
    color: #7f8c8d;
    font-size: 16px;
}

.confirmation-body {
    padding: 30px;
}

.greeting {
    font-size: 18px;
    margin-bottom: 25px;
    color: #34495e;
}

.confirmation-section {
    background: #f8f9fa;
    padding: 20px;
    border-radius: 5px;
    margin-bottom: 25px;
    line-height: 1.6;
}

.delivery-info, .next-steps {
    margin-bottom: 30px;
}

.delivery-info h2, .next-steps h2 {
    color: #2c3e50;
    font-size: 20px;
    margin-bottom: 15px;
    display: flex;
    align-items: center;
}

.delivery-info h2 i, .next-steps h2 i {
    margin-right: 10px;
    color: #3498db;
}

.address-box {
    background: #f8f9fa;
    padding: 15px;
    border-radius: 5px;
    margin-top: 10px;
    line-height: 1.6;
    border-left: 3px solid #3498db;
}

.next-steps ul {
    padding-left: 20px;
}

.next-steps li {
    margin-bottom: 8px;
    color: #7f8c8d;
}

.confirmation-footer {
    display: flex;
    justify-content: space-between;
    padding: 20px 30px;
    background: #f8f9fa;
    border-top: 1px solid #eaeaea;
}

.account-btn, .continue-btn {
    padding: 12px 20px;
    border-radius: 5px;
    text-decoration: none;
    font-weight: 500;
    display: flex;
    align-items: center;
}

.account-btn {
    background: #e3f2fd;
    color: #1976d2;
}

.account-btn:hover {
    background: #bbdefb;
}

.continue-btn {
    background: #e8f5e9;
    color: #388e3c;
}

.continue-btn:hover {
    background: #c8e6c9;
}

.account-btn i, .continue-btn i {
    margin-right: 8px;
}

@media (max-width: 600px) {
    .confirmation-footer {
        flex-direction: column;
    }
    
    .account-btn, .continue-btn {
        width: 100%;
        margin-bottom: 10px;
        justify-content: center;
    }
}
</style>
{% endblock %} 