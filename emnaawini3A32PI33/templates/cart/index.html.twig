<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ 'Nos Produits'|trans|default('Nos Produits') }} - HIVE by BEEU</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="keywords" content="Fitness, Gym, HIVE, Training">
    <meta name="description" content="HIVE - Your ultimate fitness destination by BEEU">

    <!-- Favicon -->
    <link href="{{ asset('img/favicon.ico') }}" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="{{ asset('lib/flaticon/font/flaticon.css') }}" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="{{ asset('lib/owlcarousel/assets/owl.carousel.min.css') }}" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        :root {
            --bleu-vif: #00BFFF;
            --bleu: #1E90FF;
            --bleu-fonce: #0A4D68;
            --bleu-clair: rgba(162, 190, 219);
            --bleu-moyen: #4682B4;
            --gris: #2A2A2A;
            --jaune: #FFD700;
            --shadow: 0 10px 30px rgba(30, 144, 255, 0.2);
            --shadow-dark: 0 8px 20px rgba(10, 77, 104, 0.3);
        }
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
            color: var(--gris);
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }
        .bg-dark {
            background: var(--bleu-fonce) !important;
            box-shadow: var(--shadow-dark);
            transition: background 0.3s ease;
        }
        .bg-secondary {
            background: var(--bleu-clair) !important;
            color: var(--bleu-fonce);
            border-bottom: 1px solid rgba(70, 130, 180, 0.2);
        }
        .navbar-brand {
            display: flex;
            align-items: center;
            transition: transform 0.3s ease, filter 0.3s ease;
        }
        .navbar-brand:hover {
            transform: scale(1.05);
            filter: brightness(1.1);
        }
        .navbar-brand h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-weight: 700;
            font-size: 2.5rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            text-shadow: 2px 2px 6px rgba(0, 191, 255, 0.4);
            margin: 0;
        }
        .navbar-brand img {
            max-height: 80px;
            margin-right: 15px;
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            transition: transform 0.3s ease;
        }
        .navbar-brand:hover img {
            transform: rotate(5deg);
        }
        .nav-link {
            color: var(--bleu-clair) !important;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            font-size: 1.1rem;
            padding: 10px 15px;
            transition: color 0.3s ease, background 0.3s ease;
        }
        .nav-link:hover, .nav-link.active {
            color: var(--bleu-vif) !important;
            background: rgba(0, 191, 255, 0.1);
            border-radius: 5px;
        }
        .dropdown-menu {
            background: var(--bleu-fonce);
            border: none;
            box-shadow: var(--shadow-dark);
        }
        .dropdown-item {
            color: var(--bleu-clair);
            font-weight: 400;
            transition: background 0.3s ease, color 0.3s ease;
        }
        .dropdown-item:hover {
            background: var(--bleu);
            color: #fff;
        }
        .dropdown-menu .dropdown-menu {
            top: 0;
            left: 100%;
            margin-left: 0.1rem;
            background: var(--bleu-fonce);
            box-shadow: var(--shadow-dark);
        }
        .dropdown-menu .dropdown:hover > .dropdown-menu {
            display: block;
        }
        .btn-square {
            background: var(--bleu-clair);
            color: var(--bleu-fonce);
            width: 40px;
            height: 40px;
            line-height: 40px;
            border-radius: 50%;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-square:hover {
            background: var(--bleu-vif);
            color: #fff;
            transform: scale(1.1);
            box-shadow: 0 8px 15px rgba(0, 191, 255, 0.4);
        }
        .bg-secondary h6 {
            font-family: 'Poppins', sans-serif;
            font-weight: 400;
            font-size: 0.95rem;
            margin: 0;
        }
        .bg-secondary i {
            color: var(--bleu-vif);
            font-size: 1.2rem;
            margin-right: 8px;
            transition: transform 0.3s ease;
        }
        .bg-secondary div:hover i {
            transform: scale(1.2);
        }
        .footer {
            background: var(--bleu-fonce);
            color: var(--bleu-clair);
            padding: 2rem 0;
            margin-top: 2rem;
        }
        .footer h5 {
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1.5rem;
            color: var(--bleu-vif);
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .footer p {
            font-family: 'Poppins', sans-serif;
            font-size: 0.95rem;
            margin-bottom: 0.5rem;
        }
        .footer a {
            color: var(--bleu-clair);
            transition: color 0.3s ease;
        }
        .footer a:hover {
            color: var(--bleu-vif);
        }
        .footer .btn-square {
            margin: 0 5px;
        }
        .carousel-caption {
            background: rgba(10, 77, 104, 0.75);
            padding: 25px 50px;
            border-radius: 12px;
            box-shadow: var(--shadow-dark);
            animation: fadeInUp 1s ease;
        }
        .carousel-caption h1 {
            color: var(--bleu-vif);
            font-family: 'Oswald', sans-serif;
            font-size: 3.8rem;
            font-weight: 700;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 3px 3px 8px rgba(0, 191, 255, 0.5);
            margin-bottom: 25px;
        }
        .carousel-control-prev, .carousel-control-next {
            width: 5%;
            opacity: 0.8;
            transition: opacity 0.3s ease;
        }
        .carousel-control-prev:hover, .carousel-control-next:hover {
            opacity: 1;
        }
        .carousel-control-prev-icon, .carousel-control-next-icon {
            filter: drop-shadow(0 0 8px var(--bleu-vif));
            width: 40px;
            height: 40px;
        }
        .card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 1.5rem;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin-bottom: 2rem;
        }
        .table-responsive {
            border-radius: 12px;
            overflow: hidden;
            box-shadow: var(--shadow);
        }
        .table {
            background: #fff;
            margin-bottom: 0;
            border-collapse: separate;
            border-spacing: 0;
        }
        .table thead th {
            background: var(--bleu-fonce);
            color: var(--bleu-clair);
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 15px;
            border: none;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .table tbody td {
            font-family: 'Poppins', sans-serif;
            font-size: 1rem;
            color: var(--gris);
            padding: 15px;
            vertical-align: middle;
            border: none;
            border-bottom: 1px solid rgba(70, 130, 180, 0.1);
        }
        .table tbody tr {
            transition: background 0.3s ease;
        }
        .table tbody tr:hover {
            background: rgba(0, 191, 255, 0.05);
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background: rgba(230, 240, 250, 0.3);
        }
        .btn-primary {
            background: var(--bleu);
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-primary:hover {
            background: var(--bleu-vif);
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(0, 191, 255, 0.4);
        }
        .btn-secondary {
            background: #6c757d;
            border: none;
            padding: 12px 30px;
            font-family: 'Oswald', sans-serif;
            font-weight: 600;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #fff;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }
        .btn-secondary:hover {
            background: #5a6268;
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(108, 117, 125, 0.4);
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(60px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @media (max-width: 991px) {
            .navbar-brand h1 {
                font-size: 2rem;
            }
            .navbar-brand img {
                max-height: 60px;
            }
            .nav-link {
                font-size: 1rem;
                padding: 8px 12px;
            }
            .carousel-caption h1 {
                font-size: 2.5rem;
            }
            .footer h5 {
                font-size: 1.3rem;
            }
            .table thead th, .table tbody td {
                font-size: 0.9rem;
                padding: 10px;
            }
        }
        @media (max-width: 576px) {
            .carousel-caption {
                padding: 15px 25px;
            }
            .carousel-caption h1 {
                font-size: 1.8rem;
            }
            .card {
                padding: 1rem;
            }
            .table thead th, .table tbody td {
                font-size: 0.8rem;
                padding: 8px;
            }
            .footer {
                text-align: center;
            }
            .footer .btn-square {
                margin: 5px;
            }
        }
    </style>
</head>
<body>
    <!-- Header Start -->
    <div class="container-fluid bg-dark px-0 fixed-top">
        <div class="row gx-0">
            <div class="col-lg-3 bg-dark d-none d-lg-block">
                <a href="{{ path('app_home1') }}" class="navbar-brand w-100 h-100 m-0 p-0 d-flex align-items-center justify-content-center">
                    <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                    <h1>HIVE</h1>
                </a>
            </div>
            <div class="col-lg-9">
                <div class="row gx-0 bg-secondary d-none d-lg-flex animate__animated animate__fadeInDown">
                    <div class="col-lg-7 px-5 text-start">
                        <div class="h-100 d-inline-flex align-items-center py-3 me-4">
                            <i class="fa fa-envelope"></i>
                            <h6>info@hivebybeeu.com</h6>
                        </div>
                        <div class="h-100 d-inline-flex align-items-center py-3">
                            <i class="fa fa-phone-alt"></i>
                            <h6>+21622820210</h6>
                        </div>
                    </div>
                    <div class="col-lg-5 px-5 text-end">
                        <div class="d-inline-flex align-items-center py-3">
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                            <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a class="btn btn-square rounded-circle animate__animated animate__pulse animate__infinite" href="#">
                                <i class="fab fa-youtube"></i>
                            </a>
                        </div>
                    </div>
                </div>
                <nav class="navbar navbar-expand-lg bg-dark navbar-dark p-3 p-lg-0 px-lg-5">
                    <a href="{{ path('app_home1') }}" class="navbar-brand d-block d-lg-none">
                        <img src="{{ asset('images/logo.png') }}" alt="HIVE Logo">
                        <h1>HIVE</h1>
                    </a>
                    <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                        <div class="navbar-nav mr-auto py-0">
                            <a href="{{ path('app_home1') }}" class="nav-item nav-link">Home</a>
                            <a href="{{ path('app_about1') }}" class="nav-item nav-link">About</a>
                            <a href="{{ path('app_produit_index') }}" class="nav-item nav-link">Liste Produits</a>
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Reserver</a>
                                <div class="dropdown-menu rounded-0 m-0">
                                    <div class="dropdown">
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('client_services') }}" class="dropdown-item">Services Client</a>
                                            <a href="{{ path('app_client_reservation_index') }}" class="dropdown-item">Abonnement</a>
                                        </div>
                                    </div>
                                    <div class="dropdown">
                                        <a href="#" class="dropdown-item dropdown-toggle" data-bs-toggle="dropdown">Coach</a>
                                        <div class="dropdown-menu rounded-0 m-0">
                                            <a href="{{ path('app_coach_reservations') }}" class="dropdown-item">Espace coach</a>
                                            <a href="{{ path('app_planning_index', {'coachId': 2}) }}" class="dropdown-item">Planning</a>
                                            <a href="{{ path('app_cours_index', {'user_id': 2}) }}" class="dropdown-item">Cours</a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
    <!-- Header End -->

    <!-- Carousel -->
    <div class="container-fluid p-0 mb-5">
        <div id="header-carousel" class="carousel slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img class="w-100" src="{{ asset('img/carousel-1.jpg') }}" alt="Cart" style="object-fit: cover; height: 50vh; filter: brightness(0.8);">
                    <div class="carousel-caption d-flex flex-column align-items-center justify-content-center">
                        <h1 class="text-white display-4 animate__animated animate__fadeInDown">Mon Panier</h1>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Cart Content -->
    {% block content %}
        <div class="container animate__animated animate__fadeInUp animate__delay-1s">
            <div class="card shadow-lg border-0">
                {% for message in app.flashes('error') %}
                    <div class="alert alert-danger">
                        {{ message }}
                    </div>
                {% endfor %}

                {% if fullCarts|length > 0 %}
                    <div class="p-4">
                        <h3 class="text-dark mb-4">Votre Panier (<i class="text-info font-weight-bold">{{ fullCarts|length }}</i> produit(s))</h3>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th scope="col" class="border-0 bg-light">
                                            <div class="p-2 px-3 text-uppercase">Produit</div>
                                        </th>
                                        <th scope="col" class="border-0 bg-light">
                                            <div class="py-2 text-uppercase">Prix</div>
                                        </th>
                                        <th scope="col" class="border-0 bg-light">
                                            <div class="py-2 text-uppercase">Quantité</div>
                                        </th>
                                        <th scope="col" class="border-0 bg-light">
                                            <div class="py-2 text-uppercase">Total</div>
                                        </th>
                                        <th scope="col" class="border-0 bg-light">
                                            <div class="py-2 text-uppercase">Supprimer</div>
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% set total = 0 %}
                                    {% for cart in fullCarts %}
                                        <tr>
                                            <th scope="row" class="border-0">
                                                <div class="p-2">
                                                    <div class="ml-3 d-inline-block align-middle">
                                                        <h5 class="mb-0">
                                                            <a href="#" class="text-dark d-inline-block align-middle">{{ cart.product.nomProduit }}</a>
                                                        </h5>
                                                        <span class="text-muted font-weight-normal font-italic d-block">
                                                            Catégorie: {{ cart.product.categorie.nomcategorie }}
                                                        </span>
                                                        <span class="text-muted font-weight-normal font-italic d-block">
                                                            Stock disponible: {{ cart.product.stockDispo }}
                                                        </span>
                                                    </div>
                                                </div>
                                            </th>
                                            <td class="border-0 align-middle">
                                                <strong>{{ (cart.product.prix / 100)|number_format(2, ',', '.') }}TND</strong>
                                            </td>
                                            <td class="border-0 align-middle">
                                                <a class="text-primary" href="{{ path('app_decrease_from_cart', {'id': cart.product.id}) }}">
                                                    <i class="fa fa-minus" aria-hidden="true"></i>
                                                </a>
                                                <strong>{{ cart.quantity }}</strong>
                                                <a class="text-success" href="{{ path('app_add_to_cart', {'id': cart.product.id}) }}">
                                                    <i class="fa fa-plus" aria-hidden="true"></i>
                                                </a>
                                                {% if cart.quantity > cart.product.stockDispo %}
                                                    <div class="text-danger small">Stock maximum: {{ cart.product.stockDispo }}</div>
                                                {% endif %}
                                            </td>
                                            <td class="border-0 align-middle">
                                                <strong>{{ (cart.product.prix * cart.quantity / 100)|number_format(2, ',', '.') }}TND</strong>
                                            </td>
                                            <td class="border-0 align-middle">
                                                <a href="{{ path('app_delete_from_cart', {'id': cart.product.id}) }}" class="text-dark">
                                                    <i class="fa fa-trash"></i>
                                                </a>
                                            </td>
                                            {% set total = total + cart.product.prix * cart.quantity %}
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="p-4">
    <div class="border-top pt-3">
        <div class="d-flex justify-content-between fw-bold border-top pt-2">
            <span>Total :</span>
            <span>{{ (total / 100)|number_format(2, ',', '.') }}TND</span>
        </div>
    </div>

    {% if stockError %}
        <div class="alert alert-danger mt-3">
            <i class="fas fa-exclamation-triangle me-2"></i>
            Stock insuffisant pour un ou plusieurs produits. Veuillez ajuster les quantités.
        </div>
        <button class="btn btn-secondary rounded-pill py-2 btn-block" disabled>
            <i class="fas fa-lock me-2"></i>Procéder au paiement
        </button>
    {% else %}
        <a href="{{ path('app_order') }}" class="btn btn-primary rounded-pill py-2 btn-block">
            <i class="fas fa-credit-card me-2"></i>Procéder au paiement
        </a>
    {% endif %}
</div>
                {% else %}
                    <div class="alert alert-info">
                        Votre panier est vide. <a href="{{ path('app_produit_index') }}" class="alert-link">Commencez vos achats</a>
                    </div>
                {% endif %}
            </div>
        </div>
    {% endblock %}

    <!-- Footer Start -->
    <div class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-6 text-center text-lg-start mb-4 mb-lg-0">
                    <h5>Contactez-nous</h5>
                    <p><i class="fa fa-envelope me-2"></i>info@hivebybeeu.com</p>
                    <p><i class="fa fa-phone-alt me-2"></i>+21622820210</p>
                </div>
                <div class="col-lg-6 text-center text-lg-end">
                    <h5>Suivez-nous</h5>
                    <div class="d-inline-flex">
                        <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                            <i class="fab fa-twitter"></i>
                        </a>
                        <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                        <a class="btn btn-square rounded-circle me-2 animate__animated animate__pulse animate__infinite" href="#">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a class="btn btn-square rounded-circle animate__animated animate__pulse animate__infinite" href="#">
                            <i class="fab fa-youtube"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Footer End -->

    <!-- JS Libraries -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('lib/owlcarousel/owl.carousel.min.js') }}"></script>
    <script src="{{ asset('js/main.js') }}"></script>

    <!-- Custom JavaScript -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', function(e) {
                    if (this.getAttribute('href').startsWith('#')) {
                        e.preventDefault();
                        const target = document.querySelector(this.getAttribute('href'));
                        target.scrollIntoView({ behavior: 'smooth' });
                    }
                });
            });
        });
    </script>
</body>
</html>