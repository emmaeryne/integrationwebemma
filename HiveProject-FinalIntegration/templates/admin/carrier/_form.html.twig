{{ form_start(form, {'attr': {'class': 'needs-validation carrier-form', 'novalidate': 'novalidate'}}) }}
    <div class="card shadow-sm border-0 rounded-lg overflow-hidden mb-4">
        <div class="card-header bg-gradient-primary text-white py-3">
            <h5 class="mb-0">
                <i class="bi bi-truck me-2"></i>
                {{ form_title|default('Carrier Information') }}
            </h5>
        </div>
        <div class="card-body p-4">
            <div class="row g-4">
                <div class="col-md-6">
                    <div class="form-floating mb-3">
                        {{ form_widget(form.name, {
                            'attr': {
                                'class': 'form-control',
                                'placeholder': ' ',
                                'style': 'border-radius: 8px;'
                            }
                        }) }}
                        {{ form_label(form.name, null, {'label_attr': {'class': 'form-label'}}) }}
                        <div class="invalid-feedback">
                            Please provide a valid name.
                        </div>
                    </div>
                </div>
                
                <div class="col-md-6">
                    <div class="form-floating mb-3">
                        {{ form_widget(form.price, {
                            'attr': {
                                'class': 'form-control',
                                'placeholder': ' ',
                                'style': 'border-radius: 8px;'
                            }
                        }) }}
                        {{ form_label(form.price) }}
                        <div class="invalid-feedback">
                            Please provide a valid price.
                        </div>
                    </div>
                </div>
                
                <div class="col-12">
                    <div class="form-floating mb-3">
                        {{ form_widget(form.description, {
                            'attr': {
                                'class': 'form-control',
                                'placeholder': ' ',
                                'style': 'height: 120px; border-radius: 8px;'
                            }
                        }) }}
                        {{ form_label(form.description) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer bg-light d-flex justify-content-between py-3 px-4">
            <a href="{{ path('admin_carrier_index') }}" class="btn btn-outline-secondary rounded-pill px-4">
                <i class="bi bi-arrow-left me-1"></i> Back to list
            </a>
            <button type="submit" class="btn btn-primary rounded-pill px-4 shadow-sm">
                <i class="bi bi-save me-1"></i> {{ button_label|default('Save') }}
            </button>
        </div>
    </div>
{{ form_end(form) }}

<style>
    .carrier-form .form-control {
        border: 1px solid #e0e0e0;
        transition: all 0.3s;
    }
    .carrier-form .form-control:focus {
        border-color: #3498db;
        box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.15);
    }
    .carrier-form .card {
        border: none;
        box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
    }
    .carrier-form .btn {
        transition: all 0.3s;
    }
    .carrier-form .btn-outline-secondary:hover {
        background-color: #f8f9fa;
        border-color: #dee2e6;
    }
    .carrier-form .btn-primary {
        background-color: #3498db;
        border-color: #3498db;
    }
    .carrier-form .btn-primary:hover {
        background-color: #2980b9;
        border-color: #2980b9;
    }
    .rounded-pill {
        border-radius: 50rem !important;
    }
</style>